<!--
  REGLA PERMANENTE: cada commit que cambie funcionalidad agrega UNA línea
  concisa bajo "[Sin publicar]". En cada checkpoint de versión, esas líneas
  se mueven a una nueva sección con el número de versión y la fecha.
-->

# Changelog

Contexto técnico para quien no conoce el proyecto: "Cuaderno de seguimiento"
es una app para que un profesor particular de matemática lleve el registro
de sus alumnos (exámenes, avance por tema, clases dictadas, resultados de
simulacros). El frontend (`web/app/`) es vanilla JS sin build ni bundler —
`<script>` clásicos cargados en orden fijo, ver `ARQUITECTURA.md` — empaquetado como PWA con
service worker (`sw.js`). El backend es un único proyecto Supabase
compartido (multi-tenant vía RLS), consumido con `fetch` crudo contra
Auth/PostgREST, sin SDK de `supabase-js`. El deploy es automático a GitHub
Pages en cada push a `main` que toque `web/**`. Las migraciones SQL del
backend viven versionadas aparte, en el repo privado `cuaderno-supabase`.
Los wrappers nativos (Tauri para escritorio, Capacitor para Android) también
viven en repos propios (`cuaderno-desktop`, `cuaderno-android`), ambos en
pausa.

Formato basado en [Keep a Changelog](https://keepachangelog.com/), adaptado
a una sola sección de viñetas por versión (sin subcategorías Added/Fixed/etc.).

## [Sin publicar]
- Paso 198 (huecos de verdad libres en el portal): `huecosLibresProximos14Dias()` (`helpers.js`) sólo marcaba ocupada la celda de INICIO de cada clase (`horaCeldaDe(e.time)`), así que una clase de varias horas dejaba las horas siguientes ofrecidas como libres para pedir/reservar en el portal — ahora `celdasOcupadasPorEvento()` bloquea todas las celdas que pisa según su duración real. Nuevo toggle por docente en Cuenta → Portal, "Qué horarios ven tus alumnos": "Solo horarios libres" (default, ausente = libres) o "Toda mi disponibilidad declarada" (sin cruzar contra la agenda, para el docente que prefiere acomodar los pedidos a mano), guardado en `publicado.huecosModo`. Sube el caché del service worker.

## [2.5.2] - 2026-07-21
- Paso 200 (cartelito de backend dev también en login/registro): el cartelito "⚙ backend de desarrollo" se armaba dentro de `render()` (`views-core.js`), así que sólo se veía una vez pasada la pantalla de login/registro — justo donde más hace falta no confundirse de backend si algo falla ahí. Se saca de `render()` y se inyecta directo al DOM al cargar `config.js` (si `IS_BACKEND_DEV`), fijo con `position:fixed`, visible en cualquier pantalla incluida `vAuth`/`vSetPassword`.
- Paso 199 (el límite de alto también en "Próximo"): reportado al verificar el paso 198 — el scroll acotado de "Para cobrar" funcionaba, pero como `.hoy-grid` iguala el alto de las tres tarjetas a la más alta (`align-items:stretch`), "Próximo" seguía sin tope (la sección "Mañana" lista TODAS las clases del día siguiente sin límite) y estiraba igual toda la fila, dejando a "Para cobrar" con un hueco vacío en vez de verse más chico. Mismo contenedor con scroll (`.hoy-scroll`, renombrado de `.hoy-cobros-scroll`) ahora envuelve también el cuerpo de "Próximo".
- Paso 198 (Pagos pendientes en filas, tablero acotado): la tarjeta "Para cobrar" del tablero ya no agrupa por alumno detrás de un desplegable — muestra TODOS los pendientes (clases sin cobrar, mensualidades vencidas, señas) como filas sueltas (misma forma que "Clases de hoy"), ordenadas por fecha con el más viejo arriba, cada una con su check ✓ propio (reusa `cobro-marcar-clase`/`pagos-check-pendiente`/`toggle-senia-estado`, con el mismo undo donde ya lo tenían). La lista tiene alto acotado con scroll propio (`.hoy-cobros-scroll`, ~150px) para no empujar el resto del tablero cuando hay muchos pendientes — con varios, algunos quedan para desplazar dentro de la tarjeta en vez de a la vista. Sube el caché del service worker.
- Fix (la demo se pisaba con la cuenta real): reportado al abrir `?demo=1` en una pestaña teniendo la cuenta real logueada en otra del mismo navegador — `syncNow()` (`sync.js`) era la única función de sync sin guard de `IS_DEMO` (todas las demás, `fetchPortalRow()`/`publicarPortal()`/etc., ya lo tenían). La cookie de sesión es del origen entero, no por pestaña, así que `getSes()` encontraba la sesión real desde la pestaña demo; como `load()` nunca setea `state.ownerUid` en demo, la guarda anti-fusión (paso 194) no la frenaba, y `refreshStateFromLocalStorage()` pisaba el cuaderno sintético de la demo con el cuaderno real de esa cuenta. Se agrega el guard al principio de `syncNow()`.
- Paso 197 (Check de pago y cobros en su lugar): cada fila pendiente de Pagos → Resumen (mensualidad o clases adeudadas de un alumno) tiene un botón ✓ que la marca pagada ahí mismo, sin abrir la ficha — mensual registra un pago por el pendiente exacto, clase/hora marca cobrada cada clase pendiente del mes (respeta packs y ausencias, mismo filtro que `pagoResumen()`); con undo en el toast. Mismo check en la tarjeta "Para cobrar" del tablero (antes la mensualidad sólo mandaba a "Ver en Pagos"). Todo lo de plata de una clase (monto, cobrada/pendiente, pack) se movió de la pestaña «Clases» a «Pagos» (`vClasesPagosCard`, nuevo) — «Clases» deja sólo un indicador chico que linkea a «Pagos», sin datos duplicados entre las dos pestañas.
- Paso 196 (Deuda al terminar la clase): una clase agendada (horario habitual o puntual) que ya terminó y todavía no se registró cuenta como deuda ESTIMADA a la tarifa vigente (modalidad clase/hora — mensual no genera, cobra fijo por mes) — `clasesSinRegistrarFor()`/`clasesEstimadasFor()` en `helpers.js`, ventana de `CLASE_ESTIMADA_LOOKBACK_DIAS` (21 días) hacia atrás. Se ve sin esperar a sincronizar: en el portal (`alumno.tarifaClase`, nuevo en `buildAlumnoBlock()`) se calcula en el momento contra la hora real de quien mira, con su propio renglón "a confirmar por tu docente"; en la app, en la ficha → Pagos ("Sin registrar") y en Pagos → Resumen, siempre separada de la deuda firme (nunca infla `pendienteTotalFor`, los avisos de WhatsApp ni el festejo de "al día"). "Clase pasada" arranca precargada con la ocurrencia sin registrar más reciente (fecha/tema/duración), con selector si hay más de una y un link para volver al formulario manual vacío — el acceso desde "Tu día"/tablero/agenda va directo a esa clase puntual.
- Paso 195 (Separación de cuentas fusionadas): sin cambios de código — `mantenimiento/paso195_separacion_cuentas.sql` documenta el procedimiento (inventario, restore desde `cuaderno_respaldos` o filtrado de `cuaderno.data.students` por id, limpieza de `portales.tokens_alumnos`/`tokens_grupos` huérfanos, alta de rol admin en `perfiles`) para reparar a mano, contra producción, los datos de dos cuentas que quedaron mezclados antes del fix del paso 194.

## [2.5.1] - 2026-07-21
- Fix (dos pestañas de la misma cuenta se pisaban): reportado al verificar el paso 194 — cada pestaña mantiene su propia copia en memoria de `state.students`/`catalog`, así que si una pestaña guardaba un cambio y ANTES de que le tocara sincronizar (debounce de 4s) otra pestaña de la misma cuenta sincronizaba (por ejemplo al volver a tener foco, `visibilitychange` en `sync.js`), esa pestaña vieja mergeaba la nube con SU copia desactualizada y pisaba en `localStorage` el cambio reciente todavía no subido — al refrescar la pestaña editada, volvía a aparecer como estaba antes de la edición. No era un bug nuevo de paso 194 (el mismo riesgo ya existía con la clave sin namespacear), pero se corrige ahora: `syncNow()` llama a `refreshStateFromLocalStorage()` (`helpers.js`) antes de mergear, para ponerse al día con lo último guardado en disco por cualquier pestaña antes de calcular qué subir. Sube el caché del service worker.
- Paso 194 (Aislamiento de cuentas): fix de un bug grave — dos cuentas en el mismo navegador podían fusionarse, porque el cuaderno local vivía bajo claves de `localStorage` fijas (`KEY`, `DIRTY_KEY`, `LAST_REMOTE_KEY` y otras ocho más ligadas al contenido), el logout no las limpiaba, y al loguear otra cuenta `syncNow()` mergeaba el cuaderno viejo en memoria con la nube de la cuenta nueva y se lo subía. Ahora toda clave de contenido se namespacea con el uid de la sesión (`nsKey()`, `helpers.js`) — sólo quedan compartidas las preferencias de dispositivo (tema/acento/sonidos/densidad). `storeSession()` (`auth.js`) detecta un cambio de cuenta (uid distinto al que ya estaba en memoria) y vacía todo el estado cacheado (`clearAccountState()`) antes de recargar el cache local del uid que entra; logout hace lo mismo. Migración suave de un cuaderno viejo sin sufijo: se adopta sólo si el uid que loguea coincide con el último uid visto en este dispositivo (`LAST_UID_KEY`, sobrevive al logout a propósito) — si es otro (o no se sabe, incluida la primera vez que corre este fix) se descarta sin adoptar y arranca vacío hasta el próximo sync; esto significa que, la primera vez que cada usuario existente abra la app después de este cambio, va a ver el cuaderno vacío un instante hasta que baje de la nube (necesita conexión en ese momento puntual). Guarda anti-fusión en `syncNow()`: aborta el ciclo entero si el cuaderno en memoria (`state.ownerUid`) no coincide con la sesión activa, en vez de mergear/subir. Los fetch de `cuaderno` y `portales` en `sync.js` ahora van con `user_id=eq.` explícito además de RLS (defensa en profundidad). Auditoría de RLS en `cuaderno-supabase`: `cuaderno`/`portales`/`perfiles` están bien (ningún acceso cruzado), pero se encontró que la política `"materiales: admin ve cualquier carpeta"` (`015_borrado_storage_api.sql`) deja a cualquier admin leer/descargar el contenido de los materiales privados de cualquier docente sin restricción de carpeta — **pendiente aplicar a mano** en el SQL Editor de `entreclases-dev` y luego producción una migración nueva que la reemplace por una función `security definer` que sólo exponga metadata (nombre/tamaño), no el archivo. Sube el caché del service worker.
- Paso 193 (Flujo dev + localhost + backend dev): nueva rama `dev` para todo el desarrollo (`main` sólo se mergea al cierre de una hoja, dispara sola el deploy — `deploy-pages.yml` ya sólo escuchaba `branches: [main]`, no hizo falta tocarlo). `SUPA_URL`/`SUPA_ANON_KEY` (`web/app/js/config.js` y `web/app/js/portal.js`, que las duplica por ser standalone) ahora se eligen por entorno: en `localhost`/`127.0.0.1` apuntan al proyecto Supabase `entreclases-dev`, en cualquier otro host a producción de siempre; `?backend=prod` fuerza producción desde localhost pidiendo confirmación antes. Cartelito fijo "⚙ backend de desarrollo" (`views-core.js`) visible mientras se corre contra dev. El service worker (`events.js`) no se registra en localhost (y desregistra cualquiera previo) para ver siempre el código fresco de `dev` sin pelear con la caché agresiva pensada para producción. Sección nueva en `CLAUDE.md` con las reglas de este flujo dev/main.

## [2.5.0] - 2026-07-21
- Paso 187 (Cierre y lanzamiento 2.5.0): landing (`web/index.html`) y catálogo completo (`web/funciones.html`) al día con lo real de esta serie — se suman tarjetas/ítems de reserva directa por orden de llegada, cancelar una clase desde el portal, promos de packs de catálogo en el portal, tarifa habitual y packs de clases con catálogo propio, plantillas propias de mensajes (reemplazando la mención vieja a "ocho plantillas" fija) y "Ver como alumno"; el link de videollamada ya estaba descripto correctamente en ambos y no se tocó. Cierre de versión: se mueve todo "[Sin publicar]" a esta sección, `APP_VERSION` a `2.5.0` (`web/app/js/config.js`) y se sube el caché del service worker.
- Paso 186 (QA de regresión 2.5.0): pasada de QA de toda la serie desde el paso 173 (portal, agenda por horas, verificación de cuentas, plantillas propias, tarifa default, packs de catálogo, fondo/animaciones/sonidos, modularización, mails, PWA), con foco en lo nuevo y la regresión de siempre. Tres arreglos: `republishAlumnoBlock()` (`web/app/js/sync.js`) republicaba el bloque de un alumno puntual (checkbox de "qué ve este alumno" en la ficha, o generar/renovar su llave) sin resolver `reservaModo`, así que `buildAlumnoBlock()` le borraba `misClases` aunque el docente tuviera "Reservar directo" activado — el alumno dejaba de ver sus propias clases ya reservadas en la agenda semanal del portal hasta el próximo "Publicar cambios" manual; ahora resuelve `reservaModo` con `resolveReservaModo()` antes de armar el bloque, igual que `publicarPortal()`. En el popover de edición de la agenda por horas, "Sólo esta clase" (`agenda-edit-scope-solo`/`agenda-edit-grupal-scope-solo`, `web/app/js/events.js`) reasignaba `origDate` a la fecha nueva después de guardar la excepción — pero la excepción sigue viviendo bajo la fecha ORIGINAL (`applyHorarioEdit()`), así que el siguiente render no la encontraba más y el popover volvía a mostrar el horario recurrente sin el cambio, y si encima se tocaba "Cancelar"/"Eliminar" en ese estado podía crear una excepción duplicada bajo la fecha nueva; se saca la reasignación. El total "pendiente" de Pagos (`moneySpan()`, paso 179) siempre animaba desde $0 hacia arriba sin importar si el monto subía o bajaba, al revés de lo documentado ("cuenta hacia abajo" al registrar un cobro) — `moneySpan()` ahora acepta una clave (`data-money-key`) y `animateCounters()` (`web/app/js/events.js`) recuerda el último valor mostrado por clave para animar desde ahí. Sin más hallazgos: portal (deuda/medios de pago, cancelar, promos/packs, biblioteca por unidad), verificación de cuentas (registro→aprobar→bienvenida), mails (sin restos del mail personal en ningún archivo), plantillas propias, tarifa default, fondo/animaciones/sonidos (consistentes en los tres archivos), PWA (`FILES` de `sw.js` completo, sin duplicados de función entre los nueve `views-*.js`), claro/oscuro, offline y atrás del navegador. Sube el caché del service worker.

## [2.4.5] - 2026-07-21
- Paso 183 (Mail institucional): el mail de contacto pasa a ser `contacto.entreclases@gmail.com` en vez del mail personal — nueva constante `CONTACT_EMAIL` en `config.js`, usada en `views-cuenta.js` (pantalla de cuenta rechazada); la landing (`web/index.html`), `web/funciones.html` y `web/terminos.html` no comparten `config.js` (archivos estáticos sin script propio) así que ahí el reemplazo es literal, mismo criterio que el resto de las constantes duplicadas por archivo en este repo. Grep final: `manugandini53@gmail.com` ya no aparece en ningún archivo de este repo. En el backend, `cuaderno-supabase/migraciones/029_mail_institucional.sql` redefine `enviar_mail_brevo()` con el sender/replyTo nuevo (alcanza a los seis mails del paso 181, todos funnelean por ahí); el mail viejo queda intacto en las migraciones anteriores (012/020/028) por convención de historial append-only. **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/029_mail_institucional.sql` completo. Sube el caché del service worker.
- Paso 181 (Mails Entreclases): pasada de marca sobre los seis mails transaccionales que manda el backend por Brevo (bienvenida al aprobar cuenta, aviso a admins de cuenta nueva, recordatorio de inactividad, aviso final de inactividad, resumen semanal, recordatorio de clase al alumno) — todos funnelean a través de `public.enviar_mail_brevo()` (`cuaderno-supabase`), así que el rediseño (logo "Entreclases" + tagline "Vos enseñás, del resto nos ocupamos.", colores del design system, footer con link a `https://entreclases.github.io`, `replyTo` explícito) se hizo una sola vez ahí adentro y alcanza a los seis; los que no tenían ningún link de vuelta al cuaderno (inactividad, resumen semanal, aviso a admins) suman uno a `https://entreclases.github.io/app/`. Los dos mails de Supabase Auth (confirmación de cuenta, recuperación de contraseña) no viven en SQL — quedan como pendiente manual en el dashboard (ver instrucciones del paso). **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/028_mails_entreclases.sql` completo.
- Paso 180 (views.js modular + CLAUDE.md eficiente): refactor puro, sin cambio de comportamiento. `web/app/js/views.js` (5.981 líneas) se parte en nueve archivos por dominio (`views-core.js`, `views-tablero.js`, `views-alumnos.js`, `views-ficha.js`, `views-agenda.js`, `views-materias.js`, `views-stats.js`, `views-cuenta.js`, `views-admin.js`) — sólo se movieron funciones (verificado: mismas 281 declaraciones top-level, mismo multiset de líneas no vacías, `node --check` en los nueve); las usadas por más de un dominio (mensajes de WhatsApp, `render()`, helpers de canvas, etc.) quedan en `views-core.js`. Nuevo `ARQUITECTURA.md` en la raíz con el mapa de qué archivo tiene qué vista. `CLAUDE.md` a dieta (de ~120 a ~80 líneas) con una sección nueva "Cómo trabajar barato". Actualiza el orden de `<script>` en `index.html`, la lista `FILES` de `sw.js` y las referencias a "views.js"/"seis scripts" en comentarios de `helpers.js`/`sync.js`/`events.js`/`config.js`/`portal.js`/`styles.css`/`README.md`. Sube el caché del service worker.
- Fix (fondo decorativo invisible del paso 178): en los tres archivos (`web/app/styles.css`, `web/index.html`, `web/app/portal.html`), `body` tenía su propio `background` opaco pero ningún `position`, así que nunca formaba stacking context propio — el `body::before` (la textura, con `z-index:-1`) quedaba en el stacking context raíz y el background sólido de `body` lo tapaba por completo en cualquier tema, aunque el toggle "Fondo decorativo" estuviera activado. Se agrega `position:relative` a `body` en los tres lugares (no cambia el containing block de los `position:fixed` existentes — eso requiere transform/filter, no relative — así que nav/modales siguen igual). Sube el caché del service worker.
- Fix (fondo decorativo seguía invisible tras el fix anterior — causa real): las 8 variantes de `--bg-pattern` (`web/app/styles.css`, `web/index.html`, `web/app/portal.html`, versión clara y oscura cada una) usaban entidades HTML sin escapar (`&#8747;`, `&#960;`, `&#8730;`, `&#931;` para ∫ π √ Σ) DENTRO de una data-URI de SVG puesta en `url("data:image/svg+xml,...")` — el `#` sin codificar corta ahí mismo la URI (el navegador lo interpreta como separador de fragmento), así que el SVG llegaba truncado/inválido y nunca se renderizaba como imagen, en ningún tema ni archivo (el fix anterior de stacking context era necesario pero no suficiente). Se reemplaza cada `#` de esas 8 ocurrencias por `%23` (`&%238747;`, etc.). Sube el caché del service worker.
- Paso 179 (Tercera pasada de vida): tercera y más ambiciosa pasada de festejos/sonidos, con lista cerrada de 8 momentos (registrar clase, cobrar deuda a $0, examen aprobado e hito de racha ya venían de pasos 143/155/162; se suman venden un pack de clases, la primera reserva directa recibida jamás desde el portal, dejar el checklist de "Tu día" en cero y cerrar el cierre de cuatrimestre con al menos un alumno pausado/despedido) — cada uno con su propia paleta de confetti (`fireConfetti()` ahora acepta `{colors,n,duration}` en vez de una paleta fija) y su propio sonido de `playTone()`. Sonidos: 9 timbres para 10 disparadores (`soundHito()` se reutiliza a propósito para el hito de racha y para "Tu día", ambos "cerraste bien el día"), sumando `soundPack`, `soundReserva`, `soundCuatrimestre` (el acorde más largo, de cierre), `soundWhoosh` (barrido de frecuencia MUY sutil al cambiar de vista, sólo con puntero preciso/`isDesktopLike()`, nunca en el primer render) y `soundError` (enganchado una sola vez en `toast()` para todo tono `"error"` de la app, en vez de cablearlo call-site por call-site). Microinteracciones: el press-effect de `:active` que ya tenía `.btn/.primary/.chip/.danger/.goalbtn` se extiende a `.tabbtn/.navitem/.sembtn`; las etiquetas de un alumno hacen pop-in al agregarlas (`.chip-pop`, se auto-consume); el total "pendiente" del mes en Pagos cuenta hacia abajo al registrar un cobro/pago (`moneySpan()`, variante de `countSpan()` con formato `$`); la entrada en cascada de la vista (`.view-enter`) pasa de incrementos de 25ms a 35ms; el aula (`deskPop`) pasa de .32s a .4s. Todo lo nuevo respeta `prefers-reduced-motion`/`ANIM_KEY`/`soundsOn()` igual que lo existente. (`web/app/js/events.js`, `web/app/js/helpers.js`, `web/app/js/views.js`, `web/app/js/sync.js`, `web/app/js/config.js`, `web/app/styles.css`). Sube el caché del service worker.
- Paso 178 (Fondo e identidad): la app (`web/app/`), la landing (`web/index.html`) y el portal público (`web/app/portal.html`) suman una textura decorativa muy sutil detrás de todo el contenido — un patrón de renglones de cuaderno con símbolos matemáticos dispersos (∫ π √ Σ y un compás), como SVG inline en `background-image` (data-URI, sin librerías ni CDN) con una variante clara y otra oscura por tema (`--bg-pattern` en `:root`/`.theme-dark`/`prefers-color-scheme:dark`, ya que un SVG en data-URI no puede leer variables CSS de color en tiempo real). Opacidad bien baja y afinada por superficie: 0.05 en la app y la landing, 0.028 en el portal (lo primero que ve un alumno externo). En la app es apagable desde Cuenta → Preferencias con un toggle "Fondo decorativo" (`BG_KEY`/`bgOn()`/`setBgOn()`/`applyBg()` en `config.js`, mismo patrón que "Animaciones" del paso 175 — clase `body.bg-off` que gatea la regla en `styles.css`, acción `toggle-fondo` en `events.js`); la landing y el portal no tienen preferencias, así que ahí queda siempre activo. Portal: el CSP (`img-src`) no incluía `data:`, así que hubo que sumarlo para que la textura no quedara bloqueada en silencio — ningún host externo nuevo, sigue siendo `'self' data: https://*.supabase.co`. De paso, los bancos vacíos de la vista Aula dejan el contorno punteado (leía como "falta cargar") por un borde sólido suave + sombra interior liviana (`.desk-body.empty`/`.desk-silhouette` en `styles.css`, con variables de tema así se adapta solo en oscuro) para que se lean como un asiento genuinamente vacío. Y la banner de Cumpleaños del Tablero suma el mismo selector "Mandar mensaje" que ya tenían las Alertas (`vAlertMsgPicker`, reutilizando `state.alertMsgFor` con la clave `"cumple-"+id` para no chocar con la del mismo alumno en la lista de Alertas) — antes sólo ofrecía el ícono de saludo de cumpleaños, ahora también se puede elegir un recordatorio, un aviso de pago o una plantilla propia; el aviso de cobros atrasados ya tenía ícono de WhatsApp + plantillas propias siempre visibles, se revisó y sigue andando igual. (`web/app/styles.css`, `web/app/js/config.js`, `web/app/js/views.js`, `web/app/js/events.js`, `web/index.html`, `web/app/portal.html`). Sube el caché del service worker.
- Paso 177 (Cuentas verificadas por admin): las cuentas nuevas dejan de activarse solas por confirmación de mail de Supabase — ahora las aprueba a mano un admin. `perfiles.estado` (`'pendiente'|'aprobado'|'rechazado'`, migración `027_cuentas_aprobadas.sql` en `cuaderno-supabase`) arranca en `'pendiente'` para toda cuenta que se registre de acá en más; el backfill de la propia migración marca `'aprobado'` a todas las cuentas que ya existían, así que nadie pierde acceso al aplicarla. Mientras el estado no sea `'aprobado'`, `render()` (`views.js`) muestra `vCuentaEnRevision()` en vez de la app — "Tu cuenta está en verificación" para pendiente, un mensaje neutro con el mail de contacto para rechazada — y `syncNow()` (`sync.js`) corta antes de pegarle a la red, en vez de dejar que las políticas RLS nuevas de `cuaderno`/`portales`/storage `materiales` (que ahora exigen `is_aprobado()` además de ser la propia fila) devuelvan un 403 confuso. `loadRole()` (`auth.js`) pasa a cachear también `estado` en la sesión, y los handlers de `auth-login`/`auth-signup` (`events.js`) esperan esa lectura antes de renderizar, para que una cuenta no aprobada no vea la app completa ni por un instante. Panel → Usuarios suma una sección "Pendientes de aprobación" arriba de todo con botones Aprobar/Rechazar (`admin_set_estado()`, RPC solo-admin) — aprobar dispara un mail de bienvenida con los primeros pasos; el registro de una cuenta nueva también le manda un aviso por mail a cada admin (extensión del trigger `crear_perfil()`), ambos envíos con su propio manejo de excepción para que un problema puntual de Brevo nunca bloquee un alta ni una aprobación real. No se tocó el flujo de recuperación de contraseña. **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/027_cuentas_aprobadas.sql` completo, **antes** de pushear este commit (si el JS se publica antes que la migración, `loadRole()` deja de poder leer el perfil hasta que se aplique). Sube el caché del service worker.
- Paso 176 (Tarifa default y packs de catálogo): Cuenta → Cobros suma "Tu tarifa habitual" (modalidad + monto + duración típica, `state.catalog.tarifaDefault`, `tarifaDefaultFor()` en `helpers.js`) — el alta de alumno (`vModal()`) la precarga sola y la muestra colapsada como "Tarifa: $X por clase (tu habitual) — cambiar", sin pedir tarifa si no hace falta tocarla; "cambiar" revela los campos de siempre, editables, sin tocar la tarifa de ningún alumno existente. El ajuste masivo de tarifas (paso 112, `vAjustarTarifas`) suma un check "Incluir tu tarifa habitual" para que un aumento general también actualice la default. Suma "Mis packs" (`state.catalog.packsCatalogo`, mismo patrón CRUD que las plantillas propias del paso 175): nombre, cantidad de clases, precio y vigencia opcional en texto libre — al vender un pack desde la ficha (paso 158, `vPackClasesCard`) se puede elegir uno del catálogo (prellena cantidad/precio, editable) en vez de "Personalizado" (el flujo de siempre, sin cambios). Marcar un pack "Mostrar en el portal" (apagado por defecto) lo suma a `publicado.promos` al publicar (`publicarPortal()` en `sync.js`) — se lista como promo en el portal público (`promosHtml()` en `portal.js`), visible con cualquier tipo de llave porque no es un dato de un alumno puntual; no vende ni reserva nada solo, el alumno sigue coordinando con el docente. Sin migración nueva: `promos` no está en la lista de claves que excluye `portal_publico()` en ninguna de sus tres ramas (viene devolviendo `publicado` completo menos esas claves desde hace varias versiones), así que viaja sola dentro de `data` sin tocar el backend. Retrocompatible: un cuaderno sin tarifa default o sin packs de catálogo sigue funcionando exactamente como antes. (`web/app/js/config.js`, `web/app/js/helpers.js`, `web/app/js/views.js`, `web/app/js/events.js`, `web/app/js/sync.js`, `web/app/js/portal.js`). Sube el caché del service worker.
- Paso 175 (Preferencias y plantillas propias): Cuenta → Preferencias suma un toggle de "Animaciones" (`ANIM_KEY`/`animsOn()` en `config.js`, se combina con `prefers-reduced-motion` en `prefersReducedMotion()` — alcanza cualquiera de las dos para saltar números que cuentan, barras que crecen y confetti) — el resto de lo "general" (tema, densidad, acento, sonidos, escala de objetivo, "Tu día y racha", mails) ya vivía ahí desde el paso 142. Cuenta → Mensajes pasa de una lista larga a acordeones por contexto (Cobros / Clases y recordatorios / Llaves y portal / Celebraciones / Otros, `MENSAJES_GRUPOS` en `config.js`), cada plantilla colapsada mostrando sólo su nombre y abriéndose de a una para editar (`vMensajesGrupo`/`vMensajePlantillaRow` en `views.js`, mismo patrón visual que la biblioteca del portal del paso 174). Suma "Mis plantillas": el docente crea las suyas (nombre + texto con variables `{alumno}/{materia}/{monto}/{link_pago}/{alias}/{mail}`, `state.catalog.mensajesPropios`) que aparecen junto a las generales en los tres selectores de "mandar mensaje" (ficha → Enviar WhatsApp, alertas del tablero, aviso de cobros atrasados — `vPlantillasPropiasChips()`/`waMsgPropia()`). Borrar una propia pide confirmación inline; retrocompatible (un cuaderno sin ninguna simplemente no tiene la clave). (`web/app/js/config.js`, `web/app/js/helpers.js`, `web/app/js/views.js`, `web/app/js/events.js`). Sube el caché del service worker.
- Paso 174 (Portal central): la sección "Portal" de Cuenta pasa a ser el tablero central — se le suma un acceso directo a Cobros (`vCuentaGroup("portal", ...)`), una lista compacta de "Llaves por alumno" para generar/copiar/renovar/revocar sin entrar a cada ficha (`vPortalLlavesAlumnosCard()`, acciones `portal-hub-alumno-*` en `events.js` — mismas funciones de `sync.js` que ya usaba la ficha, sólo con el id explícito en `data-id` en vez de depender de la ficha abierta) y un visualizador "Ver como alumno" (`vPortalPreviewCard()`): elegís la llave general, un alumno o una llave grupal y se previsualiza el portal público real en un `<iframe>` con esa llave (mismo `portal.html`/`portal.js` de siempre, nada duplicado — sólo carga al tocar "Cargar vista previa", con "Abrir en pestaña nueva" al lado). Además, la biblioteca por unidad pasa a acordeón en los dos lugares donde ya se agrupaba por unidad: el editor de materiales del docente (Materias → una materia, modo "Por unidad" — `vMatUnitBlock()`, mismo patrón visual que `vCuentaGroup()`, todas cerradas por defecto salvo la que abrís, recuerda cuál quedó abierta durante la sesión vía `state.materialesUnitOpenId`, "Ver materiales de esta unidad" la sigue abriendo directo) y la Biblioteca del portal público (`unitGroupsHtml()` en `portal.js`, ahora con `<details name="unitacc-...">` por materia para que sólo una quede abierta a la vez de forma nativa, y un `openUnitKey` en memoria que reaplica cuál estaba abierta cuando se re-renderiza la lista al tipear en el buscador). (`web/app/js/helpers.js`, `web/app/js/sync.js`, `web/app/js/views.js`, `web/app/js/events.js`, `web/app/js/portal.js`, `web/app/portal.html`). Sube el caché del service worker.
- Paso 173 (reserva semanal por orden de llegada): "Cómo reservan tus alumnos" (Cuenta → Portal, `vReservaModoCard`) reemplaza al on/off de "Pedir una clase" (paso 160) por tres modos guardados en `publicado.reservaModo` — Apagado, "Me piden y yo confirmo" (el flujo de siempre, sin cambios) o "Reservan directo (orden de llegada)": con llave individual, el alumno ve una agenda semanal compacta de los próximos 14 días (`reservaDirectaHtml()` en `portal.js`, agrupada por día) con los huecos libres-y-disponibles de siempre (`huecosLibresProximos14Dias()`) y, mezcladas en el mismo día, SUS propias clases ya agendadas marcadas "Tu clase" (`alumno.misClases`, siempre presente en este modo — a diferencia de `proximasClases`, no depende del opt-in "Compartir con el portal" de la ficha, ver `buildAlumnoBlock()` en `sync.js`). Tocar un hueco libre lo agenda al toque como clase puntual de 60 minutos, sin que el docente tenga que aceptar nada, vía la RPC pública nueva `reservar_clase_portal()` (migración `026_reserva_directa.sql` en `cuaderno-supabase`) — orden de llegada REAL: la RPC bloquea (`SELECT ... FOR UPDATE`) la fila de `portales` del docente para serializar dos reservas concurrentes, comprueba que el hueco sigue en `publicado.huecos` y, en la misma transacción, lo saca de ahí y agenda la clase directo en `cuaderno.data` (bumpeando el `updatedAt` del alumno para que el próximo sync del docente la traiga sin conflicto); si dos alumnos tocan el mismo hueco casi a la vez, el segundo recibe "ese horario se acaba de ocupar, elegí otro", nunca doble reserva. Convive con "Semana completa" (paso 170, ya excluida de `huecosLibresProximos14Dias()`) y con el rate limit de 3 pedidos/reservas/avisos por llave por día del paso 160 (mismo contador, sin distinguir tipo). El docente ve cada reserva nueva al instante como aviso descartable en el Tablero ("Ana reservó miércoles a las 18 hs", `vReservasDirectasCard`) — no requiere aceptar/rechazar (ya está aplicada), sólo un "Ok, listo" que la apaga (`solicitudes_clase.vista`, tipo `'directa'` nuevo en la tabla del paso 160). Demo (`?demo=1`) con el modo encendido y disponibilidad de ejemplo para mostrar huecos reales en Cuenta → Portal. (`web/app/js/helpers.js`, `web/app/js/sync.js`, `web/app/js/views.js`, `web/app/js/events.js`, `web/app/js/portal.js`, `web/app/portal.html`). **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/026_reserva_directa.sql` completo. Sube el caché del service worker.
- Paso 172 (cancelar clase desde el portal): con llave individual, "Próximas clases" pasa de mostrar sólo la más cercana a una lista tocable (`proximasClasesFor()` en `helpers.js`) — cada clase tiene un botón "No puedo ir" (si el docente lo activó) que abre en el lugar un motivo opcional y confirma, sin navegar a ningún lado. El aviso viaja por la RPC pública nueva `cancelar_clase_portal()` (migración `025_cancelacion_portal.sql` en `cuaderno-supabase`, reusa la tabla `solicitudes_clase` del paso 160 sumándole `tipo`/`kind`/`source_id`) con su propio rate limit de 3 avisos por llave por día; la clase no se cancela sola, queda "pedida" hasta que el docente resuelve. El docente la ve al instante en la misma tarjeta "Solicitudes de clase (portal)" del Tablero, ahora distinguiendo pedidos de cancelaciones (`vSolicitudClaseRow` en `views.js`) — Aceptar aplica lo mismo que cancelar esa ocurrencia a mano (`applyCancelacion()` para una clase puntual con seña, `cancelHorarioOccurrence()` para sólo esa fecha de un horario recurrente) y Rechazar deja "seguí en contacto con tu docente" en el portal del alumno al refrescar. Si hay una política de cancelación cargada (Cuenta → Preferencias), el portal la muestra antes de que el alumno confirme (`cancelPolicyTextoPublico()`, texto libre del docente o un resumen generado a partir de horas mínimas/devolución de seña). Apagable por docente en Cuenta → Portal ("Cancelar desde el portal", `vCancelarClaseCard`) — a diferencia de "Pedir una clase", arranca ENCENDIDO por defecto. Con llave grupal o general, como siempre, nada de esto se expone (`web/app/js/helpers.js`, `web/app/js/sync.js`, `web/app/js/views.js`, `web/app/js/events.js`, `web/app/js/portal.js`, `web/app/portal.html`). **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/025_cancelacion_portal.sql` completo. Sube el caché del service worker.
- Paso 171 (cobros del portal cerrados): la tarjeta de pagos del portal individual pasa a mostrar el desglose de la deuda por origen (`pendienteDesgloseFor()` en `helpers.js`: clases sin cobrar / mensualidad / señas, sólo lista los que tienen algo) y, cuando hay saldo pendiente, esa tarjeta se ubica ARRIBA de todo (antes del bloque personal) y se destaca con borde rojo — antes el alumno podía no enterarse de que debía hasta bajar a "Pagos"; sin deuda muestra "Estás al día ✓" chiquito, sin cambios. Nuevo pie "Últimas clases" con "Pagada ✓ / Pendiente" por cada una (`historialClasesPortal()`, sólo modalidad por clase/hora — mensual no se cobra clase por clase) para que se entienda de dónde sale el saldo. Auditado el flujo completo contra `portal_publico()` (ya con el `022_solicitudes_clase.sql` del paso 166 aplicado): la función reenvía el bloque del alumno tal cual viene de `publicado.alumnos`, así que los campos nuevos (`pendienteDesglose`, `historialClases`) viajan solos, sin necesitar otra migración. Con llave grupal o general sigue sin mostrarse nada de cobros, como estaba decidido. (`web/app/js/helpers.js`, `web/app/js/sync.js`, `web/app/js/portal.js`, `web/app/portal.html`). Sube el caché del service worker.

## [2.4.2] - 2026-07-20
- `APP_VERSION` a 2.4.2.
- Paso 170 (semana completa + próxima clase destacada): en la agenda semanal, chip "Semana completa"/"Reabrir semana" que marca la semana visible como cerrada (`state.catalog.semanasCompletas`, lista de lunes "YYYY-MM-DD", ausente = todas abiertas) — mientras está cerrada se ve el distintivo "Semana cerrada" en el header y `huecosLibresProximos14Dias()` salta esos días al armar los huecos que se publican al portal, así deja de ofrecerlos para pedir clase aunque haya disponibilidad declarada; no bloquea agendar una clase a mano. "Tu día" reemplaza el pendiente de "la semana que viene no tiene clases" por "¿Cerraste la semana que viene?", que ahora también desaparece con un click en "Semana completa" si es a propósito (y al tocarlo lleva directo a la semana siguiente en la agenda). El botón "Próxima clase" del selector "Registrar clase" (ficha → Clases) pasa de chip fantasma a tarjeta destacada con acento, ícono de calendario y una animación de entrada sutil, para que se note que es la acción de planificar (distinta de "Clase pasada", que sigue igual) — mismo flujo, sólo cambia la presencia visual (`web/app/js/helpers.js`, `web/app/js/events.js`, `web/app/js/views.js`, `web/app/styles.css`). Sube el caché del service worker.
- Paso 169 (agenda por horas): la duración de una clase se elige ahora con un selector de presets en horas (1 h, 1,5 h, 2 h, 2,5 h, 3 h, 4 h) más "Otra…" para minutos finos, en los seis formularios donde se cargaba (clase pasada, próxima clase, clase grupal pasada/próxima, horario semanal, "Programar clase acá" de la grilla y del día, y los popovers de edición de la agenda) — el dato guardado sigue siendo minutos, sin cambios de formato ni en el pago por hora/rentabilidad, que ya calculaban proporcional (`durationFieldHtml()`/`durationPresetChanged()` en `helpers.js`). La grilla semanal y la vista día de Agenda dejaron de crecer con el contenido: cada bloque de hora mide siempre lo mismo (`AGENDA_ROW_H`, config.js) y una clase ocupa un alto proporcional a su duración real dentro de él (una de 3 h ocupa 3 bloques, una de 1,5 h uno y medio); las que se superponen parcialmente conviven lado a lado como antes, y cuando hay más de dos superpuestas se comprimen en chips mínimos (color de materia + inicial) que al tocarlos abren un popover con el detalle completo de esas clases, reusando el popover de edición del paso 135 (`vAgendaDayEvents`/`vAgendaCompressedCluster`/`vAgendaHourListOverlay` en `views.js`, `clusterAgendaOverlaps` en `helpers.js`). Probado con clases superpuestas, un bloque comprimido con su popup, claro/oscuro y el scroll horizontal de la grilla en mobile (paso 134) intacto (`web/app/js/config.js`, `web/app/js/helpers.js`, `web/app/js/views.js`, `web/app/js/events.js`, `web/app/styles.css`). Sube el caché del service worker.

## [2.4.1] - 2026-07-20
- `APP_VERSION` a 2.4.1.
- Paso 168 ("Tu día" honesto + ficha sin link fijo): una clase de hoy recién cuenta como "sin registrar" en "Tu día" cuando ya terminó (hora de inicio + duración), no apenas llega el día — las de ayer siguen contando igual que antes; la racha (que sólo mira el backlog de ayer) no se veía afectada por esto. Se sacó el campo de link de videollamada fijo por alumno de la ficha y del alta de alumno — el link se carga por horario o clase puntual, nunca más un default por alumno (`linkVideollamadaFor()` en `helpers.js` ya no cae al link fijo); a quien ya tenía uno cargado se le muestra una última vez en Datos con aviso y botón para descartarlo, sin borrarlo solo. Verificado que el recordatorio por mail (SQL, `023_recordatorio_clases_mail.sql`) y el portal (`buildAlumnoBlock` en `sync.js`) ya resolvían el link por clase, no por el fijo — no hizo falta tocarlos. Sube el caché del service worker.
- Paso 167 (cuatro bugs chicos): renombrar una carrera ahora actualiza a todos los alumnos que la tenían cargada con el nombre viejo, así `normalizeCatalogCareers()` no le vuelve a crear una entrada fantasma en el próximo `load()`/sync — y `normName()` ahora ignora tildes al comparar nombres, para que "Ingenieria" e "Ingeniería" cuenten como la misma carrera. El lápiz para renombrar el tema de una clase registrada no respondía porque su acción cortaba con `return` antes de volver a renderizar; ahora sí. El opt-in de recordatorio por mail se apaga solo si se borra el mail del alumno. La tarjeta "Clases de hoy" del tablero ya sólo mostraba el día actual (verificado), pero el estado vacío decía "No tenés clases agendadas para hoy" en vez del texto pedido; ahora dice "Hoy no tenés clases 🎉". Área de toque de los lápices a 40×40px en móvil (`web/app/js/events.js`, `web/app/js/helpers.js`, `web/app/js/views.js`, `web/app/styles.css`). Sube el caché del service worker.
- Paso 166 (auditoría de SQL y simulacros pendientes): en el panel admin → Recursos, hint junto a los dos medidores explicando que base de datos (500 MB) y storage (1 GB) son dos recursos separados del plan gratis de Supabase, medidos de forma independiente (`web/app/js/views.js`). Auditado contra `cuaderno-supabase`: sigue pendiente de pegar a mano en el SQL Editor todo lo de los pasos 149/160/161/164 (`021_admin_stats_storage.sql`, `022_solicitudes_clase.sql`, `023_recordatorio_clases_mail.sql`, `024_planes.sql`) y los ALTER sueltos de los pasos 144/147 (`perfiles.terminos_aceptados_at`, `reportes.tipo`/`reportes.vista`) — armado un bloque único idempotente con los seis, y detectado que al ALTER de `terminos_aceptados_at` del paso 144 le faltaba su `grant update` de columna (sin eso, `registrarAceptacionTerminos()` en `sync.js` falla en silencio). Cuatro motores de mail/push siguen en modo simulacro server-side (`config_inactividad`, `config_resumen_semanal`, `config_push_clases`, `config_recordatorio_clases`, todas con columna `simulacro boolean default true`) a la espera de que se activen a mano. Sube el caché del service worker.

## [2.4.0] - 2026-07-19
- `APP_VERSION` a 2.4.0.
- Paso 164 (terreno de planes): columna `perfiles.plan` (`beta`/`individual`/`equipo`/`academia`, default `beta`, ver `024_planes.sql` en `cuaderno-supabase`), sin ningún límite todavía — helper único `tienePlan(feature)` en `config.js` (con el mapeo `PLAN_FEATURES` feature→planes, hoy vacío) siempre devuelve `true` mientras dure la beta. `loadRole()` cachea el plan propio en la sesión, listo para cuando `tienePlan()` empiece a mirarlo. En el panel admin (Usuarios), un selector por cuenta para cambiar su plan a mano (`admin_set_plan`, misma protección de sólo-admin que `admin_eliminar_usuario`) — sin ninguna mención de precios ni planes de cara al usuario. Sube el caché del service worker (`web/app/js/config.js`, `sw.js`).

## [2.3.2] - 2026-07-19
Mercado: grupales, packs, disponibilidad, pedir clase, recordatorios, resultados, cierre.
- `APP_VERSION` a 2.3.2.
- Paso 163 (fin de cuatrimestre): en Estudiantes, un aviso se sugiere solo en meses de recambio (jul/ago, nov-dic-feb, `finCuatrimestreTemporada()`) cuando hay algún alumno activo sin clases hace 30+ días (`alumnosSinClasesFinCuatrimestre()`) — descartable, reaparece a los 14 días (mismo patrón que el recordatorio de respaldo). Abre un overlay ("Cierre de cuatrimestre") con la lista de esos alumnos (toggle 30/60 días) y, por cada uno, tres acciones: Pausar (modo vacaciones del paso 114), Despedir (marca `status:"dejo"` y abre WhatsApp con la plantilla `despedida` nueva en `MENSAJES_META`) o Dejar como está (lo saca de la lista en esta sesión, sin tocar sus datos) — más un botón "Pausar a todos" para el lote completo. Pausar/despedir tienen deshacer desde el toast, igual que el resto de la app. El mismo overlay suma "Resumen del período" (clases dadas, horas, ingresos, alumnos y % de aprobados del paso 162 del último cuatrimestre vs. el anterior, `periodSummaryRange()` agregando 4 meses con el mismo criterio que "Comparar períodos" del paso 104) con un botón para descargar/compartir un PNG imprimible (`buildResumenPeriodoImageBlob()`, mismo patrón de canvas que el informe de progreso). Nada se borra: como siempre, sólo pausa o papelera. Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sw.js`).
- Paso 162 (resultados de examen y tasa de aprobados): al pasar la fecha de examen de un alumno activo sin resultado cargado, el Tablero pregunta "¿Cómo les fue?" (`pendingExamResults()`/`vExamResultPrompt`, ya existente) — Aprobó / No aprobó / No rindió + nota opcional, guardado en `s.examResults[]` ({id,date,result,grade}, identificado por (alumno, fecha exacta) para que un recuperatorio con fecha nueva vuelva a preguntar). Al marcar "Aprobó" aparece un festejo (confetti del paso 143) con botón "Felicitar por WhatsApp" (plantilla `felicitarAprobo` nueva en `MENSAJES_META`, editable desde Cuenta → Mensajes) que se queda hasta tocar "Listo", para dar tiempo a usarlo sin apuro. En Estadísticas, "Tasa de aprobación" (global y por materia, `examResultCounts()`, ya existente) suma un botón "Compartir mi tasa" que genera un PNG con el % general (`buildTasaImageBlob()`, mismo patrón de canvas a mano que el informe de progreso del paso 107) para publicidad orgánica del docente; "Comparar períodos" también suma "% de aprobados" del mes filtrado por fecha del resultado (`examResultCountsInMonth()`). La ficha del alumno (pestaña Clases) suma un historial de exámenes con borrar-con-deshacer, mismo patrón que los simulacros. Retrocompatible: ausencia de `s.examResults` en un cuaderno viejo se lee como "sin resultados". Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sw.js`).
- Paso 161 (recordatorio automático por mail al alumno): por alumno con mail cargado (paso 133) y opt-in del docente por alumno (chip "Recordarle las clases por mail" en la ficha, junto al campo de mail — sólo visible si tiene mail cargado; con el opt-in activo se ve como "✓ Recordatorio por mail activo", el indicador que pedía este paso, con un hint de a cuántas horas le llega), la noche anterior (configurable en horas, Cuenta → Preferencias → "Recordatorio por mail de clases", `perfiles.recordatorio_clases_horas_antes` default 14hs, una config por cuenta — no por alumno) le llega un mail con fecha, hora, materia, link de videollamada si hay y el nombre del docente, con el pie "avisale a {docente} si no podés ir". Motor: pg_cron cada hora (`cuaderno-supabase/migraciones/023_recordatorio_clases_mail.sql`) + `enviar_mail_brevo()` ya existente (paso 108/119) — `_proximas_clases_alumno()` proyecta horarios habituales + clases puntuales de cada alumno opt-in sobre los próximos 2 días y dispara el mail cuando la clase cae dentro de la ventana horaria configurada. Primera semana en modo simulacro (`config_recordatorio_clases.simulacro`, default true — sólo loguea, no manda), mismo patrón que inactividad/resumen semanal/push de clases; el techo diario de Brevo (300/día) se comparte entre los tres mails transaccionales del backend, priorizando las clases más tempranas si un día se llena y logueando lo que no entró (`recordatorios_clase_mail_enviados`, tipo `omitido_cupo`). **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/023_recordatorio_clases_mail.sql` completo. Sube el caché del service worker (`web/app/js/auth.js`, `sync.js`, `views.js`, `events.js`, `sw.js`).
- Paso 160 (pedir una clase desde el portal): con llave individual, y sólo si el docente activó "Permitir que pidan clases" en Cuenta → Portal (`vPedirClaseCard`, apagado por defecto), el alumno ve en una tarjeta nueva del portal los huecos libres-y-disponibles de los próximos 14 días (`huecosLibresProximos14Dias()` en `helpers.js`, cruza `disponibilidadFor()` del paso 159 con la agenda real; vacío si nunca se declaró disponibilidad) y puede pedir uno con una nota opcional. El pedido viaja por la RPC pública `pedir_clase()` (migración `022_solicitudes_clase.sql` en `cuaderno-supabase`, tabla `solicitudes_clase` nueva) — misma seguridad que el resto del portal (nunca expone token/user_id, sólo acepta una llave individual) y con rate limit de 3 pedidos por llave por día. El docente los ve en una tarjeta nueva del Tablero ("Solicitudes de clase (portal)", `vSolicitudesClaseCard`, refrescada cada ~5min junto al heartbeat vía `refreshSolicitudesClase()`): aceptar agenda la clase como clasePuntual de 60 min (`aceptarSolicitudClase()`, editable después en la ficha) y libera el hueco en el próximo recálculo; rechazar (`rechazarSolicitudClase()`) pide un motivo opcional. `portal_publico()` se redefine completa para sumar `huecos`/`pedirClaseHabilitado`/las últimas solicitudes de ese alumno (para que el portal muestre "Pedida"/"Confirmada"/"Rechazada" al refrescar) sólo con llave individual, nunca grupal ni general — de paso reconcilia el bloque de `cobros` del paso 141, cuya migración nunca había quedado commiteada en ese repo. Los huecos publicados se recalculan solos en cada sync real (`maybeSyncHuecosPortal()` en `sync.js`, mismo ciclo que la renovación de la biblioteca) para no ofrecer un horario que se acaba de ocupar. **Pendiente aplicar a mano** en el SQL Editor de Supabase: `cuaderno-supabase/migraciones/022_solicitudes_clase.sql` completo. Sube el caché del service worker (`web/app/js/helpers.js`, `sync.js`, `views.js`, `events.js`, `portal.js`, `sw.js`, `web/app/portal.html`).
- Paso 158 (packs de clases prepagos): en la ficha → Pagos, un alumno "por clase" o "por hora" puede tener un pack prepago activo (nunca "mensual") — se vende desde la nueva tarjeta `vPackClasesCard` (cantidad + precio sugerido con 10% de descuento sobre la tarifa, `packClasesPrecioSugerido()`, editable a mano) con `data-a="save-pack-clases"` en events.js, que registra la venta como un pago normal más en `s.pagos` (`tipo:"packClase"`, mismo recibo y flujo de WhatsApp que cualquier otro cobro) y agrega una entrada a `s.packsClases` ({id,fecha,total,restantes,precio,pagoId}, historial completo, no sólo el pack vigente). Cada clase que se registra después con un pack activo (`packClasesActivo()`, el último vendido con clases restantes) le descuenta una sola de forma automática (`aplicarDescuentoPack()` en helpers.js, usado tanto en "save-session" individual como en `registrarClaseGrupal` para clases grupales) dejando `c.packClaseId` en esa sesión — esa clase ya no vuelve a sumar en pendiente/cobrado clase por clase (`pagoResumen()` la excluye de "facturables"), así el pago del pack completo cuenta como ingreso el día real en que se vendió, no repartido entre las clases que cubre (con un hint aclaratorio nuevo en Pagos → Resumen y Estadísticas → Rentabilidad para que "clases dadas" vs. ingresos no se vea inconsistente). El contador "quedan N de M" se ve tanto en la ficha (chip junto a la tarjeta del pack) como al registrar una clase (`vClasePasadaForm`); al llegar a 0, `studentAlerts()` (helpers.js) suma una alerta nueva ("Se terminó el pack de N clases — ¿le vendés uno nuevo?") con su plantilla de WhatsApp propia (`packAgotado` en `MENSAJES_META`, config.js) igual que el resto de las alertas del tablero. `cobrosAtrasadosSummary()` y el export contable (`pagosCsvRows()`) también excluyen las clases cubiertas por pack de "atrasado" (ya están cobradas) y suman la venta del pack como su propia fila en la fecha real de cobro. Borrar el pago de un pack desde "Pagos registrados" borra también el pack asociado (con el mismo deshacer de siempre). Retrocompatible: ausencia de `s.packsClases`/`c.packClaseId`/`p.tipo` en un cuaderno viejo se lee como "sin pack", sin tocar nada de lo existente. Demo suma a Tobías Roldán (Álgebra, por clase) con un pack de 8 ya con 5 usadas, para que se vea "quedan 3 de 8" sin vender uno a mano. Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sw.js`).
- Paso 159 (disponibilidad del docente): la grilla semanal de Agenda (paso 134) suma un chip "Mi disponibilidad" que la pone en modo edición — tocando cualquier celda de la grilla se pinta/despinta esa hora como disponible para ese día de la semana (`toggleDisponibilidadCelda()` en `helpers.js`, `data-a="agenda-disp-toggle"` con día+hora en el dataset, mismo delegado de click que ya usaba la grilla en vez de un modelo nuevo — si la celda tiene una clase encima, el click la sigue abriendo igual que siempre, porque el `data-a` del evento está más adentro y `closest()` lo encuentra primero). Se guarda en `state.catalog.disponibilidad` (array plano de celdas sueltas `{day, hour}`, no rangos con start/end, para no tener que fusionar/partir nada al pintar una celda en el medio de un bloque — ver el comentario de `defaultDisponibilidad()` en `config.js`); ausente en catálogos viejos, se lee como "sin disponibilidad declarada" sin romper nada y sin disparar ningún aviso (`disponibilidadFor()`/`estaDentroDisponibilidad()` en `helpers.js`, mismo patrón de compatibilidad que `catalog.racha`). En modo lectura, las celdas vacías que caen dentro de la disponibilidad declarada se resaltan con un fondo sutil (`.disp-suggest`, `styles.css`) para sugerirlas como horario libre al agendar. Al programar una clase puntual desde "Programar clase acá" en la grilla (`vAgendaGridQuickForm`) se ve un aviso no bloqueante si el bloque elegido cae fuera de la disponibilidad; `addPuntualClase()` (`helpers.js`) repite el mismo chequeo al guardar y lo suma al `alert()` de aviso que ya mostraba por seña sin cobrar, así que también cubre "Programar clase acá" de la agenda mensual y "Próxima clase" desde la ficha — en ningún caso bloquea el guardado. Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `styles.css`, `sw.js`).
- Paso 157 (clases grupales): 2+ alumnos de la misma materia pueden compartir una clase (intensivos, grupitos) que aparece UNA sola vez en la agenda ("N alumnos", tarjeta propia `vAgendaEventGrupal`) en vez de una por alumno — se crea desde "+ Clase grupal" en Agenda o "Es una clase grupal" en Registrar clase (ficha), con recurrencia semanal opcional. Cada alumno sigue teniendo su propia fila en `s.sessions`/`s.clasesPuntuales`/`s.horarios` de siempre (nada de eso cambió de forma), sólo con un `grupoId`/`grupoClaseId` compartido nuevo que enlaza las filas de los mismos integrantes — así todo lo que ya leía esas listas por alumno (montoSesion, informes, portal individual) sigue funcionando sin tocarlo. Al registrar la clase se carga asistencia (vino/ausente con motivo) y tarea/monto por alumno en un único formulario (`vGrupalForm`); cobro sigue siendo el de siempre por alumno (modalidad/tarifa propia, sin "precio grupal" nuevo). Editar/cancelar una ocurrencia recurrente desde la agenda aplica siempre a todo el grupo a la vez (`applyHorarioEditGrupal`/`cancelHorarioOccurrenceGrupal`) — si sólo faltó uno, se resuelve como asistencia al registrar, no como excepción de horario. Estadísticas → Rentabilidad ya no duplica "horas trabajadas" por cada integrante de una clase grupal (dedupe por `grupoClaseId`), aunque el desglose por alumno/materia sigue sumando la hora de cada uno, a propósito. Cuenta suma "Grupos de clase" (roster de quién integra cada grupo, distinto de las llaves grupales de portal de la sección de arriba). Portal individual no necesitó cambios: ya mostraba sólo los datos propios del alumno (fecha/hora de su próxima clase), así que una clase grupal se ve igual que cualquier otra, sin nombrar a nadie más. Demo suma un trío de Álgebra con horario semanal e historial compartido, uno con una ausencia. Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sw.js`).

## [2.3.1] - 2026-07-19
Post-lanzamiento: mails, PWA instalable, Tu día y racha.
- `APP_VERSION` a 2.3.1.
- Paso 155 (Tu día y racha): tarjeta "Tu día" nueva arriba del tablero, con una checklist autogenerada de pendientes reales de hoy — clases de hoy/ayer sin registrar, cobros atrasados, exámenes a <=3 días sin recordatorio marcado, la semana que viene sin ninguna clase agendada y respaldo muy viejo (`pendingTasksToday()`, `helpers.js`) — cada ítem con su acción directa (registrar la clase, ir a Pagos, marcar "Ya avisé" del examen con el link de WhatsApp al lado, ir a Agenda, descargar el respaldo); sin pendientes muestra "Estás al día ✨". Racha "días al día" junto al título de la tarjeta (🔥 N): se recalcula una sola vez por día al abrir la app (`checkRachaDiaria()`), comparando contra el backlog de ayer — los días sin nada pendiente no rompen la racha (freeze automático), sólo un backlog real de ayer la reinicia, siempre con un mensaje sin culpa. Hitos de 7/30/100 días festejan con el confetti/sonido del paso 143 (`soundHito()`). Todo sincroniza vía `state.catalog.racha`/`catalog.examRecordatorios` (con default de compatibilidad para catálogos viejos, `defaultRacha()` en `config.js`) y es apagable desde Cuenta → Preferencias ("Mostrar Tu día y racha", `mostrarTuDia()`). `catalog.racha.historial` guarda los últimos 7 días {date,alDia} para que un futuro resumen semanal por mail pueda armar "estuviste al día X de Y días" — el envío en sí necesitaría un cambio en `cuaderno-supabase`, fuera del alcance de este repo, así que no está incluido. En el demo, racha fija de 9 días y dos tareas pendientes fijas (`pendingTasksToday()` bajo `IS_DEMO`) para que se vea sin depender del dispositivo. Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sw.js`).

## [2.3.0] - 2026-07-19
Versión de lanzamiento público gratuito.
- `APP_VERSION` a 2.3.0.
- Paso 151 (landing al día para el lanzamiento): sección de características de `web/index.html` actualizada con lo real de esta serie que todavía no tenía tarjeta propia — unidades con subunidades y material por unidad, carreras vinculadas a materias, el aula (30 bancos), fotos de perfil y sonidos/animaciones — más ajustes de texto en las tarjetas de pagos (menciona la modalidad por hora), portal (menciona el saldo/cómo pagarte de la llave individual) y agenda (menciona la edición al tocar una clase). `web/funciones.html` ya estaba al día (paso 146 la revisó contra el CHANGELOG completo); no necesitó cambios. Nada inventado: cada tarjeta nueva corresponde a una función ya shippeada y verificable en la demo.
- Paso 150 (QA y regresión de lanzamiento): pasada de QA pre-2.3.0 sin cambios de funcionalidad — revisión por código de las áreas pedidas, no un click-por-click real en navegador (no hay tooling de automatización de browser en este entorno; queda pendiente el recorrido manual real, ver "CÓMO VERIFICAR"). Sin hallazgos: los seis `js/*.js` pasan `node --check` sin errores de sintaxis; búsqueda de restos de textos viejos ("500 MB", "Cuaderno de seguimiento", dominio/repo antiguo) en `web/**` sin coincidencias — todas las referencias de storage por cuenta dicen "50 MB" consistentemente (`sync.js`, `funciones.html`, `terminos.html`) y todas las URLs apuntan a `entreclases.github.io`; `unitsFor()` (`helpers.js`) sigue con el fallback a `topics` para cuadernos sin `units` (unidades 2.0 retrocompatible, confirmado por código); `buildAlumnoBlock()`/`buildGrupoBlock()` (`sync.js`) confirman por código que "pendiente" (cobros) sólo viaja con la llave individual, nunca con la grupal ni la general; `sesIsAdmin()` sigue devolviendo `false` bajo `IS_DEMO` en todos los puntos que gatean el panel. No se encontró ni se aplicó ningún arreglo en este paso.
- Paso 149 (blindaje pre-lanzamiento): sub-panel "Recursos" del admin suma un medidor de uso de storage (bucket `materiales`, todos los usuarios sumados) contra el techo de 1 GB del plan gratis de Supabase, con la misma barra que ya existía para la base de datos, y un "Top 5 por uso de storage". El RPC `admin_stats` no traía ese dato — se extendió (`cuaderno-supabase/migraciones/021_admin_stats_storage.sql`, ya commiteado y pusheado en ese repo) con `storage_bytes` total y por usuario, leyendo `storage.objects` del bucket `materiales` vía `security definer`, sólo si `is_admin()`. **Pendiente aplicar a mano** en el SQL Editor de Supabase: pegar el contenido de esa migración (recrea `admin_stats()` con `create or replace function`, no rompe nada de lo que ya la usa). Checklist de seguridad revisado por código (no se pudo probar con dos navegadores en este entorno): RLS activo en las 15 tablas de `cuaderno-supabase` (`config_inactividad`, `config_push_clases`, `config_resumen_semanal`, `cuaderno`, `cuaderno_respaldos`, `cuentas_cerradas`, `metricas_diarias`, `metricas_horarias`, `notificaciones_inactividad`, `perfiles`, `portales`, `push_clases_enviados`, `push_subscriptions`, `reportes`, `resumenes_semanales_enviados`); `portal_publico()` nunca devuelve `token` ni `user_id` y cada tipo de llave (general/individual/grupal) recorta el `jsonb` a lo que le corresponde — probado por código contra las tres ramas de la función; llave inventada o revocada → mismo `return null` genérico en los tres casos, sin distinguir "no existe" de "existe pero deshabilitada"; regenerar una llave borra la anterior del mismo `jsonb` en el mismo `UPDATE`, así que muere al instante (no hay ventana donde las dos convivan); panel admin y RPCs admin-only (`admin_stats`, reportes, métricas) gatean en `is_admin()` del lado del servidor (RLS/`security definer`), no sólo en el front, y la columna `perfiles.rol` no es actualizable vía API por el propio usuario (`grant update` la excluye a propósito); rate limit de login: freno local de 5 intentos + bloqueo de 5 minutos (`LOGIN_MAX_ATTEMPTS`/`LOGIN_LOCK_MS`, `config.js`) además del rate-limit propio de Supabase Auth; sin secretos en el front — sólo la anon key (pública por diseño) y la clave pública VAPID (el par privado vive únicamente como secreto de la Edge Function). Exportar copia (.json) del cuaderno probado por código: exporta/importa `students` (no `catalog`, comportamiento simétrico y ya existente, no es un bug nuevo). Documentado en `CLAUDE.md` el procedimiento de rollback (revert + push + subir `CACHE` del sw + evaluar si hace falta una migración SQL inversa a mano); confirmado que el tag `v2.2.4` existe. Pasada de performance: sin `<img>` sueltas sin tamaño (todo el sitio usa SVG inline o CSS), sin links locales rotos en landing/funciones/términos, precache del sw al día. Sube el caché del service worker (`web/app/js/views.js`, `sw.js`).
- Paso 148 (SEO y compartir): `web/og-image.png` nuevo (1200×630, generado a partir de un SVG con los mismos colores/logo del design system — fondo degradé navy, marca del check en verde-azulado, tipografía del sistema) para que compartir cualquier link del sitio en WhatsApp/Instagram/Twitter muestre una tarjeta con marca en vez de texto pelado. `og:title`/`og:description`/`og:image`/`twitter:card` (summary_large_image) en `web/index.html`, `web/funciones.html` y `web/terminos.html` (cada uno con su propio título/descripción); el portal (`web/app/portal.html`) suma los mismos tags pero genéricos — "Portal — Entreclases", sin nombre de alumno ni de profe — más `<meta name="description">` y `<meta name="robots" content="noindex, nofollow">` porque es contenido por-usuario que no debe indexarse. `<link rel="canonical">` en landing/funciones/términos. `web/sitemap.xml` nuevo (landing, funciones, términos — la app y el portal quedan afuera a propósito) y `web/robots.txt` nuevo (permite landing/funciones/términos, `Disallow: /app/` cubre tanto la app como el portal, que vive ahí adentro). Favicon y manifest icons revisados: `icon-192.png`/`icon-512.png` ya eran los definitivos (marca navy+check, maskable), sin restos viejos. No toca el service worker: los archivos nuevos viven en `web/` (fuera de `web/app/`, fuera del scope `/app/` del SW) así que no hace falta subir `CACHE`.
- Paso 147 (feedback y errores): botón discreto y permanente "Sugerencias" en el pie de la barra lateral/inferior (oculto en modo demo) abre un mini-modal — tipo (Problema/Idea/Me gusta) + texto libre, con la pantalla actual (`state.view`) adjuntada sola — que guarda en la tabla `reportes` ya existente (`sendReport()`, ahora con columnas `tipo`/`vista`); 2 clicks y gracias, mismo patrón de overlay/modal e integración con el botón "atrás" del navegador que el resto de los mini-modales de la app. Banner descartable de bienvenida en el tablero durante los primeros 4 días después de crear la cuenta, invitando a lo mismo. Errores silenciosos: `window.onerror`/`unhandledrejection` (`events.js`) llaman a `logClientError()` (`sync.js`), que guarda en `reportes` con `tipo:"error_js"` sólo mensaje, un stack corto, la vista actual, la versión de la app y la plataforma — nunca datos del cuaderno ni de alumnos — con rate-limit de 5 por sesión de pestaña (`sessionStorage`), nunca en modo demo, nunca sin sesión, y todo envuelto en try/catch para que el logger jamás pueda romper la app que vigila. Panel admin → Reportes suma filtro por tipo (además del de pendiente/resuelto ya existente) y un chip con el tipo en cada fila; el ítem "Panel" del nav muestra un contador de reportes pendientes (`refreshReportesBadge()`, se actualiza junto al heartbeat existente cada ~5min y al resolver un reporte). **Pendiente aplicar a mano** en el SQL Editor de Supabase (`cuaderno-supabase`): `ALTER TABLE reportes ADD COLUMN IF NOT EXISTS tipo text NOT NULL DEFAULT 'problema'; ALTER TABLE reportes ADD COLUMN IF NOT EXISTS vista text;`. Verificado en un harness de Node que las funciones nuevas corren sin errores de referencia; falta la prueba real en el navegador (forzar un error de consola y mandar un feedback de ejemplo) una vez aplicada la migración. Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `sync.js`, `views.js`, `events.js`, `styles.css`, `sw.js`).
- Paso 146 (catálogo de funciones): `web/funciones.html` nuevo (mismo diseño/tema que la landing) — el catálogo completo de funciones reales de la app, agrupado en 10 temas (Alumnos y fichas, Clases y agenda, Cobros y pagos, Materias/carreras/materiales, Portal del alumno, Estadísticas y aula, Comunicación, Informes/contratos/recibos, Seguridad y datos, Personalización), revisado contra el CHANGELOG y las vistas para no dejar afuera nada de lo ya shippeado; nada inventado ni "próximamente". Botón "Ver todo lo que hace →" al final de las tarjetas de características de la landing, y la página cierra con el mismo CTA a crear cuenta y a la demo. Meta description propia, mismo selector de tema y responsive que el resto del sitio.
- Paso 145 (la demo definitiva: 32 alumnos, vidriera total): `buildDemoData()` (`web/app/js/helpers.js`) pasa de 14 a 32 alumnos repartidos desigual entre las 5 materias de siempre (Análisis Matemático I la más numerosa, 10-11 activos, para que "El aula" del paso 138 se vea llena; el resto entre 4 y 6) — con carreras vinculadas a materias (`careerIds`, paso 129), subunidades y material por unidad en un par de unidades (paso 128), dos alumnos pausados, ausencias con los tres motivos posibles, un cumpleaños hoy, varios exámenes próximos, objetivos con racha, etiquetas (paso 103), cuatro interesados en los cuatro estados (paso 119) y papelera con dos alumnos borrados más la materia de siempre. Agenda más poblada (horarios repartidos en distintos días y horas, dos clases a la misma hora en dos pares distintos, una próxima clase agendada) y varios meses más de historial para Estadísticas/Retención. `sesIsAdmin()` (`helpers.js`) ahora devuelve `false` siempre que `IS_DEMO` esté activo — la demo ya no podía mostrar el panel admin ni el rótulo de administrador aunque el navegador tuviera guardada una sesión de admin real, porque `getSes()` lee la cookie tal cual sin mirar el modo demo; con este único cambio (única función que gatea el panel y el rótulo) el modo demo manda siempre. Todo sigue generándose una sola vez por carga, en memoria, sin tocar localStorage ni red (`IS_DEMO`). Sube el caché del service worker (`web/app/js/helpers.js`, `sw.js`).
- Paso 144 (términos, privacidad y aviso de beta): `web/terminos.html` nuevo (mismo diseño/tema que la landing) con los términos de uso y la política de privacidad en castellano rioplatense — qué es el servicio, la etapa gratuita de prueba (sin garantía de disponibilidad), qué datos se guardan y dónde (Supabase), que no se venden ni comparten, la responsabilidad del docente sobre los datos de sus alumnos y el uso fiscal de sus cobros, los límites de almacenamiento, el derecho a exportar/pedir el borrado de la cuenta, la baja por inactividad ya existente y el mail de contacto — fechado, con nota de que puede actualizarse. Link "Términos y privacidad" en el footer de la landing y del portal, y en Cuenta → Aplicación. El registro suma un checkbox obligatorio "Leí y acepto los términos y la política de privacidad" que guarda la fecha de aceptación en `perfiles.terminos_aceptados_at` (si la confirmación de mail queda pendiente, se guarda el email en `PENDING_TERMS_KEY` y se registra recién en el primer login exitoso); las cuentas ya existentes no tienen que re-aceptar. **Pendiente aplicar a mano** `ALTER TABLE perfiles ADD COLUMN IF NOT EXISTS terminos_aceptados_at timestamptz;` en el SQL Editor de Supabase (`cuaderno-supabase`). Sube el caché del service worker (`web/app/js/config.js`, `sync.js`, `events.js`, `views.js`, `sw.js`).

## [2.2.4] - 2026-07-19
- Paso 143 (animaciones y sonidos): festejo corto (confetti liviano en canvas propio, ~1s, sin librerías) al registrar una clase, al cobrar algo que deja al alumno sin nada pendiente y al cumplir un objetivo de clase del todo — nunca en carga de página ni navegación, y siempre se salta si el sistema pide reducir animaciones. Sonidos nuevos por Web Audio API (sin archivos): "tin" al registrar clase, "ding" al confirmar ese cobro, acorde corto al objetivo cumplido — discretos y cortos, con toggle "Sonidos" en Cuenta → Preferencias (activado por defecto, apagable por dispositivo, se silencia solo con reduced-motion). Transiciones de vista de ~200ms a 280ms con easing más expresivo; modales con una leve entrada "pop" (overshoot chico, nunca sobre propiedades de layout); números del tablero y barras que crecen, un toque más lentos que antes. Landing: hero y tarjetas en cascada con más presencia (ya venían del paso 100, se les subió timing/duración). Sube el caché del service worker (`web/app/js/config.js`, `helpers.js`, `events.js`, `views.js`, `styles.css`, `sw.js`, `web/index.html`).
- Paso 142 (Cuenta ordenada): Cuenta pasa de una fila larga de tarjetas sueltas a grupos colapsables (Perfil docente, Cobros, Preferencias, Mensajes y plantillas, Portal y llaves, Datos y respaldos, Aplicación, Sesión — cada uno con una línea de descripción y abierto por defecto, nada escondido) con un mini-índice de atajos arriba que abre y scrollea al grupo elegido. Guiabilidad general: subtítulo de una línea nuevo en el encabezado de Tablero/Estudiantes/Agenda/Pagos/Materias/Estadísticas/Panel/Cuenta explicando qué se hace en cada vista, y un "Mapa de la app" nuevo en Centro de ayuda con qué hay en cada sección de la barra. Sube el caché del service worker (`web/app/js/views.js`, `helpers.js`, `events.js`, `styles.css`, `sw.js`).
- Paso 141 (cobros del docente: link de pago en el portal): Cuenta suma una sección "Cobros" (alias/CVU, link de pago de Mercado Pago, otro link — validados como https con un aviso si no lo son — y un QR opcional, mismo bucket/cuota que las fotos de perfil, achicado a máx. 512px). El portal individual de cada alumno (nunca la llave grupal ni la general) pasa a mostrar su propio saldo pendiente — dato nuevo, antes el portal nunca exponía pagos a propósito, cambio de diseño explícito de este paso — más "Formas de pagarle a {docente}" con los botones/alias/QR cargados. Nuevas variables `{link_pago}`/`{alias}` en las plantillas de WhatsApp de cobro/deuda (con "Podés pagar acá: {link_pago}" ya en el default de "Aviso de pago pendiente", si hay link cargado) y en la del recibo, que también suma "Formas de pago" a su pie (PDF y WhatsApp) si hay algo cargado. El docente demo tiene alias y link ya cargados. **Pendiente un cambio de backend que no pude redactar a ciegas** — `portal_publico()` (repo `cuaderno-supabase`) tiene que devolver `publicado.cobros` sólo al resolver una llave individual (nunca grupal/general) y no tocar nada más de lo que ya hace; no tengo el SQL actual de esa función en este repo para escribir el `CREATE OR REPLACE` sin riesgo de romper el resto (avisos, biblioteca, grupos) — compartime esa función y te paso el diff exacto (`web/app/js/config.js`, `helpers.js`, `sync.js`, `events.js`, `views.js`, `portal.js`, `sw.js`).
- Paso 140 (envío de llave grupal): "Enviar a los alumnos" en la llave grupal (Cuenta → Portal → Llaves grupales, y en el mini-modal del paso 139) abre un mini-modal con la lista de alumnos incluidos, cada uno con su botón de WhatsApp (mensaje pre-armado y editable ahí mismo, plantilla nueva "Compartir llave grupal" en Cuenta → Mensajes) y un "Marcar enviado" que persiste (`tokens_grupos[tok].enviados`, sin columna nueva) para retomar donde quedaste — con el aviso de que WhatsApp no permite un envío masivo real desde la web, esto es el máximo "uno por uno" asistido. Suma "Mandar por mail a todos" (mailto con todos los que tengan mail en CCO, nunca en Para, avisando quiénes quedan afuera), "Copiar mensaje" (uno genérico con el link) y "Copiar lista" (todos los mensajes personalizados juntos) (`web/app/js/config.js`, `helpers.js`, `sync.js`, `events.js`, `views.js`).
- Paso 139 (llaves a mano): "Compartir acceso" acerca las llaves del Portal a donde se trabaja, sin pasar por Cuenta → Portal — un mini-modal nuevo (link, copiar, WhatsApp con plantilla editable, QR, regenerar) disparable desde el header de la ficha del alumno, cada tarjeta/fila de la Lista y "Llave grupal" en el editor de cada materia; si el portal general todavía estaba desactivado o el alumno/materia no tenía llave, los genera solo antes de mostrar el modal. La ficha suma un indicador chico de si el alumno tiene llave activa y cuándo vence (vencimiento informativo a 30 días, no lo hace cumplir el backend — "Renovar" es regenerar, que corta el link anterior como siempre), guardado en `publicado.llavesAt` sin columna nueva (`web/app/js/config.js`, `helpers.js`, `sync.js`, `events.js`, `views.js`, `styles.css`).

## [2.2.3] - 2026-07-19
- Paso 138 (el aula: 30 bancos en 2 | 3 | 2): la visualización de "El aula" en Estadísticas (por materia) siempre muestra 30 bancos — 4 filas completas de 7 (dos grupos de 2 y uno de 3 en el medio, con dos pasillos) más una quinta fila de un grupo de 2 — en vez de mostrar sólo tantas tarjetas como alumnos hubiera (y "y N más" si pasaban de 30). Los alumnos de la materia llenan los bancos en orden (mismo criterio de antes: examen más próximo primero) desde arriba a la izquierda, fila por fila; si una materia tiene más de 30 alumnos activos, se agregan filas completas nuevas (mismo patrón 2|3|2) de a una, debajo, sin recortar la lista. Banco ocupado: foto o iniciales (paso 137) sobre el color de semáforo de siempre, con nombre corto abajo y tooltip con el detalle; banco vacío: silueta punteada sutil, sin acción. Entrada en cascada banco por banco (respeta `prefers-reduced-motion`), scroll horizontal en mobile si no entran los 360px. Sube el caché del service worker (`web/app/js/views.js`, `styles.css`, `sw.js`).
- Paso 137 (fotos de perfil, opcional): el docente (en Cuenta) y cada alumno (en su ficha, pestaña Resumen) pueden subir una foto de perfil opcional — se recorta cuadrada centrada, se achica a 256px y se convierte a WebP en el navegador (bajando la calidad hasta ~60 KB, `resizeImageToAvatar` en `helpers.js`) antes de subir; nunca se sube el archivo original. Se guardan en el mismo bucket privado de Storage que ya usan los materiales por materia (`materiales`), bajo `{uid}/avatars/{docente|alumno-id}.webp` — misma carpeta del usuario que ya autoriza la política RLS existente de materiales, así que no debería hacer falta una migración nueva (a diferir del `avatars/{userId}/...` del pedido original, que hubiera quedado fuera de esa política); cuentan contra la misma cuota de 50 MB (a ~60 KB son despreciables, pero se suman igual). Sin foto (o en el modo demo, que nunca inventa una imagen) cae en iniciales con un color estable por id, misma paleta que ya usan las materias. Aparece en la lista/tarjeta de alumnos, el encabezado de la ficha, la agenda (mini-avatar) y la del docente en la barra lateral (sólo escritorio) y en el portal público — ahí la URL ya sale firmada desde "Publicar cambios"/la renovación diaria existente, el portal nunca pide nada al bucket privado por su cuenta; **esto último depende de que la RPC `portal_publico()` (repo `cuaderno-supabase`) devuelva el campo nuevo `publicado.fotoDocente` sin cambios — probablemente sí, porque ya pasa `biblioteca`/`alumnos`/`grupos` sin migraciones dedicadas, pero conviene confirmarlo**. Sin conexión, subir/cambiar la foto avisa que hace falta internet (mismo patrón que materiales) en vez de encolar nada. CSP de `index.html` suma `data:` a `img-src` (para mostrar la foto ya descifrada con el token de sesión) y la de `portal.html` suma `https://*.supabase.co` (la URL firmada se muestra directo, sin token) (`web/app/js/config.js`, `helpers.js`, `sync.js`, `events.js`, `views.js`, `portal.js`, `index.html`, `portal.html`, `styles.css`).
- Paso 136 (guardar cambios en la ficha, confirmando): los campos de "datos" de Resumen y Pagos (nombre, carrera, materia, cátedra, teléfono, mail, estado, fecha de examen/inicio/cumpleaños, notas, tarifa, modalidad, tipo/monto de seña) dejan de guardarse solos al tocarlos — quedan en un borrador en memoria (`state.fichaDraft`, nunca toca `state.students` ni dispara sync) hasta confirmarlos con una barra fija abajo "Tenés cambios sin guardar — N campos tocados" con botones "Guardar cambios" (un solo `update()`/sync) y "Descartar" (vuelve al valor de antes). Cambiar de pestaña dentro de la misma ficha no pierde el borrador; salir de verdad (a otro alumno, a otra vista, cerrar sesión, o con el botón atrás del navegador) pide confirmación antes de descartarlo, integrado con el historial del paso 124. El resto de la ficha —acciones como registrar clase, cobrar, agregar horario, subir material, videollamada por defecto— sigue guardando al instante como siempre, sin pasar por ningún borrador (`web/app/js/config.js`, `helpers.js`, `events.js`, `views.js`, `styles.css`).
- Paso 135 (edición desde la agenda): clickear cualquier clase de la agenda (semana o día) abre un popover compacto para editarla ahí mismo — fecha, hora, duración, tema previsto y link, más "Registrar esta clase" (si ya pasó y no está registrada), "Cancelar / ausencia" y "Eliminar" (borra la clase puntual o el horario entero, según corresponda), y un link al nombre del alumno que va directo a su ficha. Cambiar fecha/hora/duración/link mueve la clase al instante; si la clase viene de un horario habitual recurrente, antes de aplicar pregunta si el cambio va "sólo a esta clase" (genera una excepción puntual sobre esa fecha, sin tocar el resto de la recurrencia — `horario.exceptions`, nuevo campo opcional, retrocompatible con horarios viejos sin excepciones) o "a todas las de este horario" (cambia el horario recurrente entero). El aviso de superposición ya existente se recalcula igual dentro del popover. Se cierra con Esc, click afuera o el botón "Cerrar", e integra con el historial del navegador del paso 124 (`web/app/js/helpers.js`, `events.js`, `views.js`).
- Paso 134 (grilla semanal completa): la vista Semana de Agenda pasa de tarjetas apiladas por día a una grilla horaria de verdad — los 7 días como columnas, siempre las 7 visibles, con bloques fijos de 08:00 a 22:00 (extendidos ese día puntual si hay una clase fuera de ese rango), hoy resaltado y una línea sutil en la hora actual. Varias clases a la misma hora conviven lado a lado (mismo criterio de `.agenda-hour-slot` del paso 90, reusando `vAgendaEvent()` tal cual); clases de más de una hora muestran su duración en la tarjeta en vez de ocupar varias filas, para no complicar el layout. Clickear un bloque vacío abre "Programar clase acá" (alumno/hora/duración, mismo flujo que ya tenía la agenda mensual) con el día y la hora del bloque precargados. En mobile, donde los 7 días no entran en 360px, la grilla scrollea horizontal (`.week-scroll`) y centra la columna de hoy al abrir la vista, en vez de perder días. La vista por día del paso 90 (agenda mensual) y el aviso de superposición no cambian (`web/app/js/views.js`, `events.js`, `helpers.js`, `styles.css`).
- Paso 132 (clase pasada o próxima): "Registrar clase" en la ficha del alumno (pestaña «Clases») pasa a un desplegable inicial con dos caminos — "Clase pasada" (el registro completo de siempre: tema, tarea, duración, objetivo, cobro, ausencia; el selector de fecha ya no deja elegir después de hoy) o "Próxima clase" (sólo agendar: fecha —no antes de hoy—, hora, duración, tema previsto opcional y link opcional; queda en la agenda y, cuando la fecha pasa, aparece para registrarla como clase dada, mismo flujo que ya usaba la agenda para las clases pendientes). El concepto "Clases puntuales" como card/flujo aparte desaparece de la ficha: sigue siendo el mismo dato (`s.clasesPuntuales`, con un `topic` nuevo y opcional) mostrado ahora como "Próximas clases" — sólo lista y permite cancelar/borrar, agregar se hace desde "Registrar clase" → "Próxima clase" (`web/app/js/views.js`, `events.js`, `helpers.js`).
- Paso 133 (alta simple y avanzada): el alta de alumno nuevo queda en dos niveles — lo visible por defecto es nombre, materia/pack, carrera, teléfono, tarifa y modalidad (alta en segundos); un desplegable cerrado "Opciones avanzadas" agrupa el resto de lo que la ficha ya soporta (mail de contacto —campo nuevo, opcional, `s.email`—, cátedra/universidad, cumpleaños, fecha de examen, link de videollamada, tags y notas iniciales, más un mini "¿Cobrás seña?" con tipo/monto) — mostrar/ocultar esa sección y el sub-bloque de seña toca el DOM directo en vez de re-renderizar el modal entero, para no perder lo ya tipeado en otros campos (mismo motivo que ya evitaba re-renderizar en "+ nueva" carrera). La política de cancelación quedó afuera a propósito: es una configuración global (Cuenta → Avisos), no por alumno. El mail nuevo aparece también en Resumen de la ficha (con un chip "Escribir por mail") y se suma como variable `{mail}` a las plantillas de WhatsApp que tienen alumno (`web/app/js/config.js`, `views.js`). Después de crear un alumno, el toast ofrece "Completar ficha" para saltar directo a su ficha en vez de navegar ahí siempre de una (`web/app/js/views.js`, `events.js`, `helpers.js`).

## [2.2.2] - 2026-07-19
- Paso 131 (tema de clase editable + genéricos): el "tema principal" de una clase ya registrada se puede editar desde el historial de la ficha (lápiz junto al tema, mismo patrón de edición inline que renombrar una unidad) sin perder clases viejas con un tema que ya no está en el catálogo. El selector de tema (al registrar y al editar) agrupa las unidades/subunidades de la materia en un optgroup "Unidades" y siempre ofrece, en un optgroup "General", los genéricos "Ejercicios"/"Teoría" además de los ya existentes "Nivelación"/"Repaso / parciales viejos" (`topicOptionsHtml()` en `web/app/js/helpers.js`, `GENERIC_TOPICS` en `config.js`) — el tema de una clase sigue siendo independiente del avance por unidad (`s.topics`), nunca se derivó de ahí.
- Paso 130 (pago por hora): nueva modalidad de cobro "Por hora" además de "Por clase" y "Mensual" (`s.modalidad==="hora"`) — reutiliza `s.tarifa` como el valor por hora y calcula el precio de cada clase automáticamente como tarifa × duración en horas (redondeado al peso, mismo `classDurationHours()` que ya usaba Rentabilidad para la duración proporcional), con un campo "Monto (opcional)" al registrar la clase para pisar ese cálculo cuando una clase puntual vale distinto (`s.sessions[].monto`, ausente/`null` = usar el cálculo automático). `hasPagos()`, `pagoResumen()`, el export contable CSV, cobros atrasados, estadísticas de ingresos y rentabilidad ya suman esta modalidad (vía el nuevo helper `montoSesion()`); "Ajustar tarifas" en lote ya la incluía sin cambios, porque nunca filtró por modalidad. Dos alumnos de ejemplo nuevos en la demo (`web/app/js/helpers.js`, `views.js`, `events.js`).
- Paso 129 (carreras vinculadas a las materias): `catalog.careers` pasa de `string[]` a `{id, nombre}[]` para tener identidad propia — `normalizeCareers()`/`normalizeCatalogCareers()` (`web/app/js/helpers.js`) son las únicas funciones que leen el formato viejo (string suelto) y de paso recuperan cualquier carrera escrita a mano en la ficha de un alumno que nunca se hubiera agregado al catálogo; se llaman junto a `normalizeCatalogUnits()` en cada punto donde ya se normalizaba el catálogo (`load()`, merge remoto y restaurar respaldo en `sync.js`, datos de demo). `s.career` de cada alumno sigue siendo texto plano sin cambios, así que los filtros existentes no se tocan. Cada materia suma `careerIds` (N:M, chips para vincular/desvincular desde su editor) y la lista de Materias sale con un toggle "Todas"/"Por carrera" que agrupa sin duplicar materias (con su bucket "Sin carrera asignada"). En Materias → Carreras ahora se puede renombrar una carrera sin perder el vínculo con sus materias; en la ficha y en el alta de alumno nuevo, el campo Carrera pasa de `<select>` a texto libre con autocompletado (`<datalist>`) más un botón "+ nueva" para crear una carrera sin salir del formulario, y al elegir una materia vinculada a alguna carrera se sugiere/autocompleta esa carrera si el campo todavía está vacío (`web/app/js/views.js`, `events.js`).
- Paso 128 (material por unidad): cada material puede enlazarse opcionalmente a una unidad de su materia (`unitId`, vacío/ausente = "General", retrocompatible con materiales viejos) — selector "General"/"Por unidad" en el editor de Materiales, con un bloque propio por unidad (y su propio subir) más un bloque "General" (`web/app/js/views.js`); tocar el chip de conteo de materiales de una unidad en la lista de unidades salta directo a su bloque (`events.js`, scroll al final de `render()`). El portal agrupa la Biblioteca por unidad dentro de cada materia (nombre/orden denormalizados al publicar, sin migración de backend: `publicarPortal()`/`republishGrupoBlock()` en `sync.js` ya arman ese JSON del lado del cliente) — `web/app/js/portal.js`, `web/app/portal.html`.
- Paso 127 (Unidades 2.0): las unidades de una materia pasan de string suelto a `{id, nombre, orden, subunidades:[{id,nombre}]}` — `normalizeUnits()`/`normalizeCatalogUnits()` (`web/app/js/helpers.js`) son la única función que lee el formato viejo (string) y siempre devuelve objetos; se llaman al cargar el cuaderno, al mergear el catálogo remoto y al restaurar un respaldo (`sync.js`), nunca en cada lectura — de ahí en más el catálogo en memoria son objetos y el cuaderno se re-guarda en formato nuevo recién cuando el usuario guarda algo. El avance de cada alumno (`s.topics`) sigue clave por el nombre de la unidad, no por su id, así que ningún cuaderno viejo pierde avance registrado. En Materias, cada unidad ahora se renombra (lápiz o doble click), se borra (con confirmación sólo si algún alumno ya tiene avance registrado ahí) y admite subunidades (un nivel, mismo patrón editar/borrar/reordenar); unidades y subunidades se reordenan con flechas subir/bajar siempre visibles (nada de drag & drop, un infierno en mobile) y ese orden se respeta en el avance, los informes, el portal y el selector de "tema principal" al registrar una clase (que ahora también lista las subunidades). Enter en "nueva unidad"/"nueva subunidad" agrega, limpia el campo y deja el foco listo para seguir cargando en cadena. Los botones "Agregar unidad"/"Agregar subunidad" pasan a botón primario con el acento del paso 125 (`web/app/js/events.js`, `views.js`, `sync.js`, `styles.css`).
- `APP_VERSION` a 2.2.2.

## [2.2.1] - 2026-07-18
- Paso 123: `CLAUDE.md` con las reglas permanentes del proyecto (estructura de `web/`, `portal.html`, backend en `cuaderno-supabase` sin aplicar SQL solo, reglas inquebrantables de caché/sync-auth/CHANGELOG/commits/CDN/idioma, y la regla de cerrar cada respuesta con un checklist "CÓMO VERIFICAR").
- Paso 124: botón "atrás" del navegador (`web/app/js/events.js`, `views.js`): cada cambio de vista principal (tablero, estudiantes, ficha, agenda, pagos, materias, estadísticas, cuenta, panel, informe/contrato/recibo) suma una entrada de historial chica ({vista, id}) en vez de sacar de la app; "atrás" navega entre vistas y, desde la primera, sale como cualquier página. Los overlays a pantalla completa (alta de alumno, buscador, elegir alumno del FAB, QR) se cierran con "atrás" en vez de navegar, patrón estándar en apps móviles. F5 recarga en la misma vista (se restaura desde el hash de la URL). Sin cambios en login/recuperación ni en el modo demo (`?demo=1` se mantiene en la URL).
- Paso 125 (fricciones visuales): "Primeros pasos" ahora se descarta en `catalog.onboardingDismissed` (sincroniza entre dispositivos en vez de vivir en localStorage) y, al completar los 4 pasos, se despide sola con un toast ("¡Listo! Ya diste los primeros pasos") y no vuelve a aparecer salvo que se reactive desde Ayuda (`web/app/js/helpers.js`, `views.js`, `events.js`). Arreglado el salto de scroll al editar un campo de la ficha (más notorio en "Cátedra / universidad"): el re-render que dispara el evento "change" ahora restaura la posición de scroll además del foco, para cualquier campo (`events.js`). Botones primarios/verdes (Iniciar sesión, Crear cuenta, Guardar, CTA del hero) pasan a fondo `--accent-strong` (#0F766E) con texto blanco, con contraste AA (5.47:1) verificado en tema claro y oscuro, tanto en la app como en la landing — el acento claro sigue igual para bordes, links y estados (`web/app/styles.css`, `web/index.html`).
- Paso 126 (textos y accesos): revisados los límites de almacenamiento en toda la app/landing/portal — ya estaban al día en todos lados (100 archivos por materia, 10 MB por archivo, 50 MB totales por usuario, todos leídos dinámicamente de `config.js`); el único "500 MB" del proyecto es el uso de base de datos del panel admin (`SUPABASE_FREE_LIMIT_BYTES`, un recurso de Supabase distinto de los materiales, correctamente rotulado). Acceso discreto a "Papelera (N)" al pie de Estudiantes, visible sólo si hay algo para restaurar, sin mover el acceso existente en Cuenta (`web/app/js/views.js`). En las alertas del tablero, botón "Mandar mensaje" siempre disponible por alumno (recordatorio, aviso de pago o mensaje libre en la ficha) además del ícono rápido atado al motivo puntual de la alerta (`web/app/js/views.js`, `events.js`, `helpers.js`).
- `APP_VERSION` a 2.2.1.

## [2.2.0] - 2026-07-18
- Landing al día (`web/index.html`): sección de características actualizada con lo real de esta serie — llaves cortas/grupales y avisos del portal, demo completo, agenda con vista por horas e "Imprimir semana", y las mejoras de las etapas U y V que ya estaban shippeadas: recordatorio push con la app cerrada, ajuste de tarifas en lote, ausencias con motivo, modo vacaciones (pausar alumno), link de videollamada por clase, mensajes de WhatsApp con plantillas editables (cumpleaños incluido) e "Interesados" para no perder consultas. Mismo diseño y selector de tema de siempre, sin tocarlos.
- Arreglo: fuga de avisos por la llave general del portal (`cuaderno-supabase` `019_portal_avisos_filtro.sql`): QA de regresión encontró que `portal_publico()` nunca había recibido el filtrado por destino que el paso 105 dejó pendiente ("la migración... queda para aplicar a mano", nunca se escribió) — la llave GENERAL (pensada para compartirse ampliamente, ej. el QR del aula) devolvía `publicado.avisos` completo, exponiendo el texto de avisos dirigidos a un alumno puntual o a una materia. De paso, las llaves individual y grupal nunca mostraban ningún aviso (ni siquiera los generales) porque nadie arma ese campo en su respuesta; y `subjectId` en el bloque del alumno individual no se sacaba pese a que un comentario en `sync.js` aseguraba que sí. Los tres se corrigen en la misma función: cada tipo de llave recibe sólo los avisos que le corresponden según su destino, y `subjectId` ya no llega al navegador.
- Arreglo: ausencias contaban como clase dada en varios lugares que el paso 113 no tocó (`web/app/js/helpers.js`, `views.js`, `sync.js`): `cobrosAtrasadosSummary()` (banner "para cobrar" del tablero) las ofrecía para cobrar igual que una clase real; el informe de progreso (texto, PDF/PNG para compartir con padres) las contaba y listaba como "Clase"; "tarea de la última clase" (WhatsApp y portal) podía mostrar la nota interna de una ausencia con esa etiqueta; y la alerta "dos clases seguidas sin tarea" más `lastSessionDate()` (orden de la lista, "Salud del mes") podían enmascararse o confundirse con ausencias intercaladas. Todos ahora excluyen sesiones marcadas `ausente`, igual criterio que ya tenían `pagoResumen()`/`classesInMonth()`/el CSV contable.
- Arreglo: "Convertir en alumno" de un interesado no chequeaba duplicados (`web/app/js/helpers.js`, `events.js`): a diferencia de cualquier otra alta manual, podía crear una segunda ficha en silencio si esa persona ya era alumno. Ahora pasa por `findDuplicateStudent()` igual que el resto y avisa en vez de duplicar.
- Arreglo: borrado de cuenta desde el panel admin con Storage a medias, en silencio (`web/app/js/sync.js`, `views.js`): si fallaba el borrado de los materiales de esa cuenta, la cuenta se eliminaba igual (comportamiento correcto — no bloquear por eso) pero sin avisar nada; ahora el mensaje final dice si quedaron archivos huérfanos por limpiar a mano.
- Arreglo: restos del nombre viejo en superficies visibles para el usuario (`cuaderno-supabase` `020_fix_nombre_mails.sql`, `INSTRUCCIONES.md`): el renombre a Entreclases (paso 96) nunca había llegado a los mails transaccionales (Brevo) — remitente, asunto y cuerpo del aviso de inactividad y del resumen semanal seguían diciendo "Cuaderno de seguimiento" en el mail real que le llega a cada docente. También el título de la guía de uso.
- `APP_VERSION` a 2.2.0.

## [2.1.4] - 2026-07-18
- Interesados (paso 119, `web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`): en Estudiantes, pestaña nueva "Interesados" — mini lista de espera (`state.catalog.interesados`, aparte de `state.students` a propósito: alguien que sólo preguntó todavía no es un alumno, sin ficha ni historial) con nombre, contacto, materia de interés (texto libre, no atada al catálogo), nota y estado con ciclo fijo (Consultó → En charla → Arranca → No arranca, se avanza tocando el chip). "Convertir en alumno" (`convertirInteresado()` en helpers.js) crea la ficha de verdad precargando nombre/contacto/materia/nota y lo saca de la lista, mismo patrón de un solo `save()`/`render()` que el ajuste de tarifas en lote (paso 112). Borrar un interesado usa el mismo patrón de toast con "Deshacer" que el resto de la app. Pensado para no perder una consulta que llega por WhatsApp mientras se decide si arranca. Precache del service worker en `cuaderno-v111`.
- Agenda imprimible (paso 118, `web/app/js/views.js`, `events.js`): en la vista Semana de Agenda, botón "Imprimir semana" — documento imprimible de la semana que se esté viendo (mismo `state.agendaWeekOffset`), agrupado por día con hora, alumno y materia de cada clase, mismo patrón de "documento" que informe/contrato/recibo (`.informe-bar`/`.informe-doc`, con la identidad de Entreclases) para que salga igual de prolijo pegado en la heladera o en el aula. Sin datos nuevos ni backend — reusa `agendaWeekEvents()` tal cual. Precache del service worker en `cuaderno-v110`.
- Plantillas de mensajes (paso 117, `web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`): en Cuenta, nueva sección "Mensajes" que centraliza TODOS los textos que la app arma para WhatsApp (próxima clase, recordatorio de clase del paso 111, tarea de la última clase, recordatorio de examen, cumpleaños del paso 115, coordinar pago y aviso de deuda) más el texto del recibo — ocho plantillas editables (`MENSAJES_META` en config.js), cada una con sus variables documentadas y un botón "Restaurar" propio que sólo repone esa plantilla puntual. `mensajeTexto()`/`fillTemplate()` (helpers.js) hacen la sustitución `{variable}`; el recibo usa además `fillTemplateLines()`, que borra la línea entera de "Saldo restante"/"Docente" cuando no aplican (evita un renglón suelto sin tener que dejar esa lógica fuera de la plantilla). La firma del recordatorio de clase ("Nos vemos — nombre") queda fuera del texto editable a propósito, igual que antes del paso 111 — no es una variable, se agrega sola con el nombre del docente para no duplicar ese dato en dos lugares. La tarjeta específica de recordatorio de clase que había quedado suelta en Cuenta desde el paso 111 se unificó acá adentro; su plantilla vieja (si ya la habían personalizado) se seguía respetando hasta pisarla desde la sección nueva. El informe de progreso queda afuera — es un reporte armado con varias secciones, no un mensaje corto con variables. Precache del service worker en `cuaderno-v109`.
- Link de videollamada (paso 116, `web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sync.js`, `portal.js`): campo "Link de videollamada" opcional por alumno (default, ficha → Clases) y también por horario habitual o clase puntual — el propio de la clase pisa al del alumno (`linkVideollamadaFor()`, nunca se combinan). `agendaRangeEvents()`/`nextClaseForStudent()` lo llevan colgado de cada evento, así que aparece solo donde corresponde sin recalcular nada aparte: botón "Entrar a la clase" en la Agenda (clases de hoy/futuras, no en las ya pasadas) y en el tablero Hoy ("Clases de hoy" y "Mañana"). En el portal individual, se suma a "Próxima clase" (`buildAlumnoBlock()` en sync.js) — sólo con llave individual, nunca en la general ni en la grupal, mismo criterio de siempre. Precache del service worker en `cuaderno-v108`.
- Cumpleaños (paso 115, `web/app/js/config.js`, `helpers.js`, `views.js`): campo opcional "Cumpleaños" en la ficha (`s.birthDate`, fecha de nacimiento — sólo importa mes-día, `isBirthday()` en helpers.js). El tablero Hoy muestra arriba de todo una tarjeta "Cumpleaños" con quién cumple hoy y mañana entre los alumnos activos, con saludo pre-armado por WhatsApp si tiene teléfono cargado (mismo patrón `waLink`/`hasPhone` de siempre). Ícono de línea nuevo (`ICON_CAKE`, mismo criterio del set unificado del paso 73 — nunca un emoji suelto). Precache del service worker en `cuaderno-v107`.
- Pausar alumno (paso 114, `web/app/js/views.js`, `events.js`): en la ficha, botón "Pausar" (con fecha de vuelta opcional) y "Reanudar" — una capa guiada sobre el estado "Pausado" que ya existía (`STATUS_META`, seleccionable a mano desde el select de Estado): un alumno pausado ya quedaba afuera de la agenda, "para cobrar" y "salud del negocio" porque esas vistas sólo miran `status==="activo"` (`agendaRangeEvents`, `cobrosAtrasadosSummary`, `vSaludDelMes`), así que lo nuevo acá es la acción en sí (`vFichaPausa`, guarda `s.pausaHasta` opcional) y que en Estudiantes → "Todos" los alumnos en pausa se separan a una sección aparte "En pausa" al final de la lista (mezclados se perdían entre los activos) en vez de aparecer intercalados — en cualquier otro filtro (incluido "Pausado" mismo) la lista sigue igual que antes. Cambiar el estado a mano desde el select (en vez de "Reanudar") también limpia `pausaHasta` para no dejar una fecha de vuelta colgada. "Reanudar" no toca nada del historial — mismo alumno intacto. Precache del service worker en `cuaderno-v106`.
- Ausencias (paso 113, `web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`): al registrar una clase, elegir "Dada" (default, igual que antes) o "Ausente" — con motivo (avisó con tiempo / avisó tarde / no avisó) y si se cobra o no, sugerido según el motivo (avisó con tiempo → no se cobra; avisó tarde o no avisó → sí, mismo criterio de fondo que la política de cancelación de señas) pero modificable con un toque antes de guardar. Una ausencia queda en `s.sessions` igual que una clase dada (mismo array, misma fecha) pero se excluye de todo lo que cuenta "clases dictadas" — `pagoResumen()` (cobro por clase), `classesInMonth()` (horas de Rentabilidad) y el CSV contable — para no inflar cobros ni horas con clases que no pasaron; los recibos ya emitidos no se tocan. En la ficha (Resumen) y en Estadísticas (por materia, este mes): % de asistencia y cantidad de ausencias (`asistenciaStats()`); alerta nueva en el tablero/lista si un alumno acumula 3+ ausencias en el mes. Precache del service worker en `cuaderno-v105`.
- Ajuste de tarifas (paso 112, `web/app/js/helpers.js`, `views.js`, `events.js`): en Pagos, pestaña nueva "Ajustar tarifas" — aumento porcentual o monto fijo a la tarifa de varios alumnos activos de una, con redondeo elegible ($100/$500/$1000 o sin redondeo) y vista previa por alumno (tarifa actual → nueva) agrupada por materia, cada uno incluido por defecto y destildable uno a la vez o por materia entera (`toggle-tarifa-ajuste-materia`). `applyTarifaAjuste()` aplica todos los cambios con un solo guardado/render y le agrega a cada alumno una línea a `s.tarifaHistorial` (fecha, de cuánto a cuánto), visible en su ficha → Pagos ("Historial de tarifas") — sólo lo llena este ajuste en lote, no un cambio manual del campo Tarifa. Clases ya registradas y recibos ya emitidos guardan su propio monto y no se recalculan con este ajuste. Precache del service worker en `cuaderno-v104`.
- Recordatorio WhatsApp (paso 111, `web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`): botón "Recordar por WhatsApp" junto a cada clase de hoy (tablero Hoy y Agenda) y de mañana (tablero Hoy → "Próximo" y Agenda) — mismo patrón de link `wa.me` ya usado en toda la app (`waLink`/`hasPhone`, sin API de WhatsApp), nada nuevo del lado del backend. El texto es editable en Cuenta (`state.catalog.waRecordatorioClase`, mismo patrón que política de cancelación/docente — sincroniza vía catalog) con variables `{alumno}`, `{materia}`, `{dia}` y `{hora}` (`waMsgRecordatorioClase()` en views.js); la firma con el nombre del docente se agrega sola al final si está cargado en la tarjeta "Datos del docente" de más arriba, para no duplicar ese dato en dos campos editables. Precache del service worker en `cuaderno-v103`.
- Export a calendario (paso 110, `web/app/js/helpers.js`, `views.js`, `events.js`): el botón de exportar la agenda a `.ics` (ya existía, fijo a las próximas 4 semanas) pasa a llamarse "Exportar a mi calendario" y exporta el período que se está viendo — la semana actual en la vista Semana, la grilla del mes elegido en la vista Mes (`agendaIcsRangeForView()` en helpers.js, mismo rango que ya se dibuja en pantalla) — y se suma el botón a la vista Mes, que todavía no lo tenía. Formato del evento ajustado a "Clase: Ana — Matemática" (antes "Clase con..."). Hint nuevo abajo del botón explicando cómo importarlo (Google Calendar, Outlook, calendario del teléfono). Sigue siendo 100% local (Blob descargado, iCalendar estándar, sin ningún backend nuevo). Precache del service worker en `cuaderno-v102`.
- Repetir semana (paso 120): evaluado y salteado a propósito — los horarios habituales (`s.horarios`, ver `agendaRangeEvents()` en helpers.js) ya son la recurrencia equivalente: una vez cargados repiten esa clase todas las semanas sin ninguna acción manual, mejor que copiar clases puntuales semana a semana. No se tocó código.
- `APP_VERSION` a 2.1.4.

## [2.1.3] - 2026-07-17
- Push de recordatorios (paso 108, `web/app/js/config.js`, `helpers.js`, `auth.js`, `sync.js`, `views.js`, `events.js`, `sw.js`; `cuaderno-supabase`: `migraciones/018_push_clases.sql`, `supabase/functions/enviar-push/index.ts` nuevo): switch "Recordarme las clases del día" en Cuenta (`perfiles.notif_clases_dia`, apagado por defecto) que suscribe el service worker a `PushManager` y guarda la suscripción del dispositivo en `push_subscriptions` (RLS: cada quien ve/toca solo la suya). A diferencia del aviso de cobros ya existente (`vRecordatoriosCard`, sólo suena con la app abierta), este llega aunque la app esté cerrada — un `pg_cron` matutino (11:00 UTC = 08:00 Argentina) junta los docentes opt-in con clases previstas ese día (`clases_hoy_para_push()`, sobre `s.horarios`/`s.clasesPuntuales` del jsonb del cuaderno) y dispara vía `pg_net` la Edge Function `enviar-push`, que firma cada envío con VAPID y lo manda como web push real — el firmado/cifrado no era viable en PL/pgSQL puro, así que esa parte vive en Deno (primer uso de Edge Functions en este proyecto) en vez de en la base. La clave privada VAPID es un secreto de la Edge Function (`supabase secrets set`), no de Vault (Vault sólo guarda el secreto que autentica la llamada del cron a la función, `push_cron_secret`, porque la función la invoca `pg_net` sin ningún usuario logueado). El service worker (`sw.js`) muestra la notificación y, al tocarla, enfoca una pestaña ya abierta o abre una nueva en el tablero; suscripciones vencidas (404/410 al mandarles un push) se borran solas. Arranca en modo simulacro del lado del servidor (`config_push_clases.simulacro`, sólo loguea en `push_clases_enviados`) hasta activarlo a mano, mismo patrón que el resumen semanal (paso 56/80). No disponible en Tauri/Capacitor (no registran service worker) ni en iOS salvo la PWA instalada (Safari 16.4+). Precache del service worker en `cuaderno-v100`.
- Arreglo: avisos del portal sin filtrar por destino (`web/app/js/sync.js`): al revisar el `portal_publico()` real del backend (paso 105) se vio que la llave general expone `publicado` completo salvo `alumnos`/`grupos` — así que un `publicado.avisos` sin filtrar habría dejado ver, incluso con la llave general, avisos dirigidos a una materia o a un alumno puntual. `buildAlumnoBlock()` ahora suma `subjectId` (nunca mostrado, sólo para que el backend pueda filtrar avisos "por materia" por alumno) — el filtrado en sí queda en la migración de `portal_publico()` que acompaña este cambio, aplicada aparte en `cuaderno-supabase`. Precache del service worker en `cuaderno-v99`.
- Informe como imagen (paso 107, `web/app/js/views.js`, `events.js`, `helpers.js`): en el informe del alumno, botón "Compartir como imagen" — genera un PNG del resumen (encabezado con Entreclases, alumno, período, avance por unidades, clases del período, % de objetivos cumplidos, comentario del profesor si hay) dibujado a mano en un `<canvas>` (`buildInformeImageBlob()`, Canvas 2D nativo, sin ninguna librería) y lo comparte con la hoja nativa del teléfono (`navigator.share` con archivo) o, si no está disponible, lo descarga directo — ideal para mandarlo por WhatsApp a los padres. Usa siempre los colores de marca fijos (navy/teal), nunca el acento elegible del paso 106, porque es un documento que sale hacia terceros. Precache del service worker en `cuaderno-v98`.
- Color de acento (paso 106, `web/app/js/config.js`, `views.js`, `events.js`): en Cuenta → Apariencia → "Color de la app", 6 acentos predefinidos (teal por defecto, azul, violeta, verde, naranja, rosa), cada uno con su par claro/oscuro y su propio `--on-accent` (texto bien oscuro sobre el acento, nunca blanco — ninguno de estos tonos llega a 4.5:1 con blanco en ningún tema, mismo criterio que el teal original del paso 75). Se aplica pisando `--accent`/`--accent-dark`/`--accent-soft`/`--on-accent`/`--shadow-accent` como variables inline en `<html>` (`applyAccent()`), recalculadas al cambiar de tema o cuando el sistema pasa de claro a oscuro solo estando en "Automático" — así llega a todos los botones/pestañas/barras que ya usaban esos tokens, sin tocar `styles.css`. Preferencia local (como el tema/densidad, no viaja en el JSON del cuaderno). El portal y la landing no cargan este JS, así que se quedan con el acento de marca fijo. Precache del service worker en `cuaderno-v97`.
- Avisos en el portal (paso 105, `web/app/js/sync.js`, `views.js`, `events.js`, `helpers.js`, `web/app/portal.html`, `js/portal.js`): en Cuenta → Portal, nueva tarjeta "Avisos para el portal" — mensaje corto con destino (todos los alumnos, una materia o un alumno puntual), guardado en `publicado.avisos` (mismo jsonb existente, sin columna nueva) vía `saveAvisos()` (mismo patrón optimista que `togglePortalHabilitado()`). En el portal del alumno aparecen arriba de todo como tarjeta destacada con fecha (`avisosHtml()` en `portal.js`). El filtrado por destino (individual = los suyos + los de su materia + los generales; grupal = los de su materia + los generales; general = sólo los generales) lo tiene que resolver `portal_publico()` del lado del backend — es la única forma segura de no exponerle a un alumno un aviso ajeno —, así que este paso no lo aplica solo: la migración con el SQL de esa función queda para aplicar a mano en `cuaderno-supabase` (no vive en este repo). Precache del service worker en `cuaderno-v96`.
- Comparar períodos (paso 104, `web/app/js/helpers.js`, `views.js`, `events.js`): en Estadísticas, pestaña "Comparar períodos" — elegís dos meses (selects, con atajos "Este mes vs. anterior" y "Mismo mes, año pasado") y se ven lado a lado ingresos cobrados, clases dadas, horas dictadas, alumnos con clase ese mes y % de objetivos cumplidos (`statsPeriodSummary()`, `goalCountsInMonth()`), cada métrica con una barra por período (valor siempre visible al lado, nunca sólo la barra) y la diferencia en verde/rojo con flechita (`trendBadge()`, ya usado en Retención — % de objetivos usa diferencia en puntos en vez de % relativo). Todo calculado del historial local (sesiones, pagos, objetivos); "alumnos con clase" es la aproximación a "activos" de un mes pasado que da el historial, ya que el estado del alumno es un valor presente sin registro mes a mes. Sin ningún dato nuevo guardado. Precache del service worker en `cuaderno-v95`.
- Etiquetas por alumno (paso 103, `web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`): tags libres en la ficha de cada alumno ("ingreso", "secundaria", "online", "verano"…), guardadas en `catalog.tags` (`{id,label,color}`, JSON del cuaderno) y referenciadas desde `s.tagIds`. El color sale de la misma paleta accesible de `SUBJECT_COLOR_KEYS` (paso 73), repartido por el menos usado. Se agregan/quitan como chips en la ficha (`getOrCreateTag()` matchea sin importar mayúsculas, así "Verano" y "verano" no se duplican — el input tiene datalist con las existentes), se muestran también como chips en la lista de alumnos, se puede filtrar la lista por etiqueta y encontrarlas escribiendo su nombre en la búsqueda global. Precache del service worker en `cuaderno-v94`.
- Densidad compacta (paso 102, `web/app/js/config.js`, `views.js`, `events.js`, `styles.css`): nueva preferencia en Cuenta → Apariencia → Densidad ("Cómoda"/"Compacta", default Cómoda), guardada en `localStorage` (mismo criterio que el tema, no viaja en el JSON del cuaderno). Compacta aplica `body.density-compact` y reduce paddings/alto/margen (vía variables CSS `--row-pad`, `--log-pad`, `--agenda-event-pad`, `--agenda-day-pad`/`-minh`, `--cal-cell-pad`/`-minh`) en las listas largas — estudiantes, clases, pagos, agenda y materiales —, sin tocar tamaño de letra (nunca baja de 14px). Funciona igual en claro/oscuro y mobile. Precache del service worker en `cuaderno-v93`.
- `APP_VERSION` a 2.1.3.

## [2.1.2] - 2026-07-17
- Animaciones (paso 100, `web/app/styles.css`, `web/app/js/views.js`, `helpers.js`, `events.js`, `web/index.html`, `web/app/portal.html`, `js/portal.js`): pasada de animación por landing, app y portal, CSS puro. Transición de fade al cambiar de vista en la app (sólo al navegar de verdad, no en cada re-render por datos); entrada escalonada de tarjetas/filas/columnas de gráfico con `animation-delay`; los números grandes del tablero "Hoy" y de Estadísticas cuentan hasta su valor (`countSpan()`, siempre muestran el valor correcto de entrada por si el conteo no llega a correr); barras y gráficos (semáforo, avance, retención, aprobación, avance por unidad del portal) crecen una sola vez al entrar en pantalla vía `IntersectionObserver`; hero de la landing con entrada propia y tarjetas de características con hover de elevación + sombra; feedback de scale al presionar botones ampliado. Todo con `transform`/`opacity` (nunca propiedades de layout) y apagado por completo con `prefers-reduced-motion: reduce`. Precache del service worker en `cuaderno-v92`.
- Mudanza a entreclases.github.io (paso 101, `web/app/js/config.js`, `INSTRUCCIONES.md`, `CLAUDE.md`): el repo de Pages se transfirió a la organización `entreclases` y se renombró a `entreclases.github.io` — `RELEASES_API`/`DOWNLOADS_URL` y los links de la guía de uso apuntan al dominio nuevo. `cuaderno-supabase` sigue en `manugandini53-design`, sin cambios. Los links del portal ya repartidos siguen funcionando porque se arman con `location.href` (no hardcodeados) — la URL vieja se redirige aparte, desde un repo `manugandini53-design.github.io` nuevo, sin código en este repo. Precache del service worker en `cuaderno-v90`.
- Actualización asistida (paso 99, `web/app/sw.js`, `web/app/js/events.js`, `views.js`, `config.js`, `helpers.js`): el service worker deja de hacer `skipWaiting()` solo al instalar — una versión nueva se queda "esperando" hasta que la app la pide. La app chequea actualizaciones al cargar, cada `SW_UPDATE_CHECK_INTERVAL_MS` (1h) y al volver a la pestaña (`registration.update()`); cuando hay una lista, muestra un aviso arriba de todo ("Hay una versión nueva de Entreclases — Actualizar") que manda `SKIP_WAITING` por `postMessage`, espera `controllerchange` y recarga una sola vez, sin tocar sesión ni datos. En Cuenta → Versión: número de versión actual y botón "Buscar actualización" que fuerza el chequeo y avisa si ya está al día. Nueva pregunta en el centro de ayuda sobre por qué a veces se ve una versión vieja y qué hacer. Precache del service worker en `cuaderno-v89`.
- Auditoría móvil (paso 98, `web/app/styles.css`, `web/app/index.html`, `web/app/portal.html`, `web/index.html`): revisada toda la web en 320/360/390/414/430px. Inputs/selects/textareas a `font-size:16px` (evita el zoom automático de iOS al enfocar); botones principales (`.primary`, `.danger`, `.tabbtn`, chips-botón) y el botón de descarga del portal a ~44px de alto en mobile; textos de contenido que quedaban por debajo de 14px (hints, notas, alertas, FAQ, estados vacíos, texto de informe/contrato/recibo, subtítulos de fila) subidos a 14px. Soporte de zonas seguras (`viewport-fit=cover` + `env(safe-area-inset-*)`) en la barra inferior de la app, el FAB, los toasts y el header sticky de la landing. Arreglado el desborde del header de la landing en pantallas angostas (brand + botón ya no se salían, ahora envuelven). Precache del service worker en `cuaderno-v88`.
- Plataformas próximamente (paso 97, `web/index.html`): la sección "Usala donde quieras" pasa de una sola tarjeta destacada con la leyenda "vuelven más adelante" a tres tarjetas iguales estilo Características — Web (Disponible, con su botón), App móvil y App de escritorio (Próximamente cada una) —, sin ningún texto de "vuelven más adelante". Revisado el resto de la landing: sigue al día con lo que hace la app 2.1.x. Precache del service worker en `cuaderno-v87`.
- Renombre a Entreclases (paso 96, `web/index.html`, `web/app/index.html`, `web/app/portal.html`, `web/app/manifest.webmanifest`, `web/app/js/views.js`, `helpers.js`, `events.js`, `sw.js`): el nombre visible pasa de "Cuaderno de seguimiento" a "Entreclases" en landing (título, header, hero, footer), app (barra lateral, login, manifest), portal y en los documentos generados (informe, contrato, recibo, notificación de cobros, `.ics`). Tagline nueva "Vos enseñás, del resto nos ocupamos." en el hero de la landing, como subtítulo del login y discreta en el pie del portal. Nada interno (claves de `localStorage`, tablas, buckets, prefijo del caché) se tocó. Precache del service worker en `cuaderno-v86`.
- `APP_VERSION` a 2.1.2.

## [2.1.1] - 2026-07-17
- Demo completo y visible (paso 95, `web/app/js/helpers.js`, `sync.js`, `views.js`, `web/index.html`): el cuaderno ficticio de `?demo=1` pasa de 8 a 14 alumnos (uno en la papelera) en 5 materias, y ahora usa casi todas las funciones de la app: agenda con clases superpuestas a propósito, deudas/pagos/señas/mensualidades variados, costos cargados (Rentabilidad con números reales), contratos y recibos de ejemplo, materiales simulados (compartidos y no), una materia en la papelera, varios meses de historial de clases y pagos para que Estadísticas y Retención no se vean vacías, y un portal ya activado (llave general, dos llaves individuales y una grupal). `buildDemoData()` se reconstruye entera en cada carga con fechas relativas a hoy, así nunca se ve vieja. De paso, `loadPortal()`/`fetchPortalRow()`/`patchPortalRow()`/`publicarPortal()`/`signMaterialUrl()`/`loadMateriales()`/`uploadMaterial()`/`deleteMaterial()`/`downloadMaterial()` (`sync.js`) ahora se saltean la red por completo en modo demo — antes intentaban pegarle igual al backend sin sesión real, y abrir Cuenta en la demo directamente rompía la vista (`ses.resumenSemanal` sin chequear `ses` nulo, arreglado en `views.js`). En la landing, "Ver demo" pasa de link chico a botón grande junto a "Abrir la aplicación" en el hero (ya había una mención en Características). Precache del service worker en `cuaderno-v85`.
- Arreglo: la llave grupal no mostraba los materiales ya compartidos de su materia (`web/app/js/sync.js`): al crear o editar una llave grupal, `republishGrupoBlock()` reusaba `publicado.biblioteca` — que sólo se arma al tocar "Publicar cambios" general — así que una llave grupal creada antes de tocar ese botón (o antes de compartir un archivo nuevo) quedaba con la biblioteca vacía. Ahora firma directo los materiales marcados "Compartido" de esa materia, sin depender de la publicación general. De paso, `maybeRenewPortalLibrary()` (renovación diaria de links) y `removeFromPortalBiblioteca()` (al dejar de compartir/borrar un archivo) ahora también alcanzan la copia firmada aparte que vive dentro de `publicado.grupos[materiaId].biblioteca`, para que no queden links vencidos o huérfanos ahí. Precache del service worker en `cuaderno-v84`.
- Llave grupal del portal (`cuaderno-supabase` `017_llave_grupal.sql`, `web/app/js/sync.js`, `views.js`, `events.js`, `portal.js`): sumada a la llave general y a la individual por alumno, ahora se puede generar una llave por materia — al crearla, la app muestra los alumnos activos de esa materia con chips para marcar a cuáles incluye (editable después, sin regenerar la llave). Quien entra con esa llave ve la biblioteca de esa materia y las próximas clases/exámenes del grupo (fechas sueltas, sin nombres) — nunca notas, pagos ni avance de ningún alumno en particular, mismo criterio de seguridad que el portal general (`portal_publico()` sigue sin exponer tokens ni user_id, errores genéricos). Nueva columna `portales.tokens_grupos` (mapa llave→{materiaId, alumnos}) y `publicado.grupos[materiaId]`, armado del lado de la app (`buildGrupoBlock()`); crear/regenerar/borrar/editar alumnos desde Cuenta → Portal → "Llaves grupales", con copiar link y QR igual que las otras llaves. Llaves grupales de 10 caracteres, mismo generador que paso 93. Precache del service worker en `cuaderno-v83`.
- Llaves cortas del portal (`web/app/js/sync.js`, `portal.js`, `cuaderno-supabase` `016_llaves_cortas.sql`): las llaves nuevas (general y por alumno) pasan de 48 caracteres hex a 10, generadas con `crypto.getRandomValues` sobre un alfabeto sin caracteres confundibles (sin 0/O/1/l/I) — más fáciles de escribir o dictar a mano. `portal.js` baja su mínimo de validación de 20 a 8 caracteres, y `portal_publico()` (backend) baja el mismo mínimo, que si no se quedaba en 20 y bloqueaba de entrada a cualquier llave nueva. Las llaves largas ya repartidas siguen funcionando exactamente igual — el cambio sólo afecta a lo que se genera o regenera de acá en adelante, nada se migra ni se invalida. Precache del service worker en `cuaderno-v82`.
- Límites de materiales (`web/app/js/config.js`, `sync.js`): `MATERIAL_MAX_COUNT` de 20 a 100 archivos por materia y `MATERIAL_MAX_TOTAL_BYTES` de 20 MB a 50 MB por usuario (entre todas sus materias); `MATERIAL_MAX_BYTES` (10 MB por archivo) queda igual. Barra de uso, mensajes de límite alcanzado y el "máx." junto al selector de archivo ya salían de estas constantes — muestran los números nuevos sin tocarlos aparte. Precache del service worker en `cuaderno-v81`.
- Escala única en el cierre post-clase (`web/app/js/helpers.js`, `views.js`, `events.js`): el cierre de objetivo ("¿Se cumplió el objetivo?") mostraba a la vez Sí/A medias/No y un % opcional — ahora se elige una sola escala en Cuenta ("Escala de valoración: Simple o Porcentaje", default Simple) y el formulario de cierre muestra sólo la elegida. Por dentro sigue guardándose lo mismo de siempre (`objetivoResult:{estado,pct}` — Sí=100/A medias=50/No=0 en Simple; en Porcentaje el % elegido se traduce a un estado con los mismos umbrales, `estadoFromPct()`), así que estadísticas, informes y portal siguen funcionando igual con el historial viejo y el nuevo mezclados, sin migrar nada. Precache del service worker en `cuaderno-v80`.
- Agenda del día por bloques de hora (`web/app/js/views.js`, `styles.css`): la vista de un día (Agenda → Mes → tocar un día) pasa de listar las clases sueltas a una grilla vertical de bloques por hora, con scroll (desde la primera hora con clase, o 8-22 si no hace falta más rango). Cada clase aparece dentro de su bloque como tarjeta con alumno, materia (con su color, paso 73) y duración; si hay más de una en la misma hora se muestran todas lado a lado (columnas en escritorio, apiladas en móvil), nunca superpuestas ni ocultas. De paso, el aviso de superposición (ya existente en la vista Semana) ahora también se calcula en la vista de un día, que antes no lo mostraba. Clic en una clase sigue igual que antes. Precache del service worker en `cuaderno-v79`.
- Orden de tabulación en formularios (`web/app/js/helpers.js`, `events.js`, `views.js`): arreglado el bug de fondo — un campo con `change` (blur, por ej. al tabular) disparaba un `render()` sincrónico que reconstruía todo el DOM antes de que el navegador terminara de mover el foco al siguiente campo, así que Tab caía a `<body>` en vez de avanzar (confirmado con Playwright en un navegador real, ya que jsdom no simula el avance de foco por Tab). Ahora ese `render()` se difiere un tick para que el Tab nativo mueva el foco primero con el DOM viejo intacto, y recién ahí se reconstruye restaurando el foco al equivalente del campo que quedó enfocado — corrige el orden de Tab/Shift+Tab en todos los formularios auto-guardados de la app (datos del docente, ficha, catálogo, políticas, etc.), sin tocar su lógica de datos. Sumado Enter-para-enviar (`data-enter`, ya existente en login/registro) a los formularios de alta que no lo tenían: nuevo estudiante, registrar clase, registrar simulacro, agregar horario/clase puntual, registrar pago, agregar unidad/carrera/materia, crear pack. Revisados los componentes custom (chips, toggles, swatches): ya son todos `<button>` nativos, focuseables y operables por teclado de por sí. Precache del service worker en `cuaderno-v78`.
- Navegación y barra lateral (`web/app/js/views.js`, `events.js`, `styles.css`): el logo de arriba a la izquierda ("check" + "Cuaderno") es ahora un botón que lleva al tablero. Nuevo ítem "Cerrar sesión" al final de la barra (sidebar en escritorio, barra inferior en mobile) con confirmación simple antes de salir — el botón de Cuenta sigue igual, sin confirmación. El botón de tema, que quedaba visualmente suelto fuera del pie de la barra en escritorio, ahora vive dentro del mismo contenedor que "Cerrar sesión", alineado con el resto de los ítems. Auditados los accesos directos del tablero "Hoy" ("Ver agenda completa", "Ver en Pagos", etc.): ya navegaban bien a sus vistas, sin cambios de código ahí. Precache del service worker en `cuaderno-v77`.
- Borrado de cuentas vía Storage API (`cuaderno-supabase` `015_borrado_storage_api.sql`, `web/app/js/sync.js`, `views.js`, `events.js`): Supabase dejó de permitir `DELETE` directo sobre `storage.objects` ("use storage api instead"), así que `admin_eliminar_usuario`/`_eliminar_cuenta` ya no tocan Storage — sólo borran las tablas propias. El panel admin, antes de llamar al RPC, lista y borra por Storage API los archivos de la carpeta del usuario (políticas nuevas: un admin puede listar/borrar cualquier carpeta del bucket `materiales`). El cierre automático por inactividad deja la carpeta huérfana en vez de guardar la service_role key en Vault; nuevo botón "Limpiar archivos huérfanos" en Usuarios la barre comparando el bucket contra las cuentas vivas. Precache del service worker en `cuaderno-v76`.
- `APP_VERSION` a 2.1.1.

## [2.1.0] - 2026-07-17
- Landing al día (`web/index.html`): sección de características actualizada con lo real de la serie 2.x — panel "Hoy" rediseñado, buscador global, pagos con recibos numerados, export contable a Excel, portal con QR para el aula, agenda con color por materia, estadísticas con retención y salud del negocio, papelera con deshacer, resumen semanal por mail y modo demo — sobre las que ya estaban (informes, WhatsApp, materiales, modo oscuro, respaldos automáticos); mismo diseño y selector de tema de siempre, sin tocarlos. `APP_VERSION` a 2.1.0, precache del service worker en `cuaderno-v75`.

## [2.0.7] - 2026-07-17
- QR del portal (`web/app/js/qrcode.js` nuevo, `views.js`, `events.js`, `styles.css`, `index.html`, `sw.js`): botón "Ver QR" junto a "Copiar link" del portal general (Cuenta) y del portal por alumno (ficha) — abre un overlay con el código generado al momento, en grande, para escanear en clase. Usa `qrcode-generator` de Kazuhiko Arase (MIT, sin dependencias) vendorizado íntegro en un solo archivo local (`js/qrcode.js`, cargado antes que el resto); sin CDN en runtime, respeta la CSP existente (`script-src 'self'`). Precache del service worker en `cuaderno-v74`.
- Export contable (`web/app/js/helpers.js`, `views.js`, `events.js`): botón "Exportar CSV" en Pagos → Resumen, con un selector de rango (este mes / últimos 3, 6 o 12 meses, terminando en el mes elegido arriba) — una fila por movimiento (fecha, alumno, materia, concepto clase/mensualidad/seña, monto, estado) más un resumen por mes al final. Generación y descarga 100% locales (`buildPagosCsv()` + Blob), sin backend; BOM UTF-8 y separador `;` para que Excel en español lo abra bien de entrada. Precache del service worker en `cuaderno-v73`.
- Modo demo (`web/app/js/config.js`, `helpers.js`, `views.js`, `web/index.html`): `?demo=1` carga un cuaderno ficticio completo en memoria (`buildDemoData()`) — 8 alumnos variados con clases, pagos con deudas (mensualidad pendiente, seña pendiente), horarios de la semana, objetivos de clase, un examen próximo y materiales simulados solo visuales — sin cuenta, sin sync y sin escribir nada en `localStorage` ni en el backend (`save()` no hace nada en demo, `ensureToken()` ya fallaba sin sesión de por sí, `load()` arranca directo desde `buildDemoData()` en vez de leer `localStorage`). Banner permanente arriba "Modo demostración — los cambios no se guardan" con botón "Crear mi cuenta" que sale del demo recargando sin el parámetro. Link discreto "Ver demo, sin crear cuenta" en la landing. Precache del service worker en `cuaderno-v72`.
- Recibos de pago (`web/app/js/helpers.js`, `views.js`, `events.js`, `config.js`): al marcar una clase como cobrada (desde la ficha o desde el aviso de cobros del tablero), registrar un pago mensual o marcar una seña como cobrada, se genera un recibo numerado (`2026-001`, `2026-002`… por año, contador en `catalog.reciboSeq`) con un toast "Ver recibo" para abrirlo al toque; documento imprimible con el mismo patrón visual que informe/contrato (`vRecibo()`, reusa `.informe-*`), con botón "Copiar texto para WhatsApp" y "Enviar por WhatsApp" directo si el alumno tiene teléfono cargado. Cada recibo emitido queda guardado en `s.recibos` y listado en la pestaña Pagos de la ficha ("Recibos emitidos") para volver a verlo cuando haga falta. Precache del service worker en `cuaderno-v71`.

## [2.0.6] - 2026-07-17
- Resumen semanal por email (`web/app/js/auth.js`, `sync.js`, `events.js`, `views.js`; `cuaderno-supabase`: `migraciones/014_resumen_semanal.sql`): switch "Recibir el resumen semanal por mail" en Cuenta (`perfiles.resumen_semanal`, apagado por defecto) que guarda la preferencia al toque (optimista, con rollback si falla el guardado). En el backend, `enviar_resumenes_semanales()` corre los domingos a la noche (hora Argentina) vía pg_cron y le arma a cada docente no inactivo un resumen simple — clases dadas, cobrado y pendiente, próximos exámenes/objetivos sin evaluar y alumnos que se están enfriando, calculado directo sobre el jsonb de `cuaderno` — y lo manda por `enviar_mail_brevo()` (reusada del paso 56). Arranca en modo simulacro (sólo loguea el HTML que mandaría, sin mandar nada) hasta activarlo a mano en el SQL Editor.
- Retención y salud del negocio (`web/app/js/views.js`): dos secciones nuevas al final de Estadísticas — "Retención" con la duración promedio de un alumno (meses de la primera a la última clase registrada) y altas/bajas por mes de los últimos 6 meses (altas por fecha de inicio, bajas por alumnos en estado "dejó", gráfico de barras con signo — verde crece, rojo baja — igual criterio que el histórico de Rentabilidad); y "Salud del mes" con activos recientes vs. "enfriándose" (3–6 semanas sin clase) vs. inactivos (6+ semanas o nunca), con un chip de alerta por cada alumno que se enfría o está inactivo para abrir su ficha directo. Se calcula todo sobre el historial ya guardado, sin backend nuevo; mismo estilo de gráficos CSS/SVG con valores siempre visibles (criterio del paso 57). Precache del service worker en `cuaderno-v69`.
- Ficha con pestañas y listas filtrables (`web/app/js/views.js`, `events.js`, `helpers.js`): la ficha de alumno pasa de 4 pestañas largas a 6 acotadas — Resumen (vistazo rápido con próxima clase/deuda/racha de objetivos/avance, más unidades, datos de contacto, informe/contrato y borrar), Clases (registrar + horarios + puntuales + simulacros), Pagos (tarifa/modalidad + pagos mensuales + seña), Objetivos (racha + historial con resultado) y Materiales (lectura de lo compartido con ese alumno) — Portal queda igual que antes. Lista de Estudiantes con dos filtros nuevos — deuda (debe/al día) y orden (por examen/por última clase/por nombre), con la deuda de cada uno como chip en la fila cuando corresponde — sumados al buscador y contador de resultados que ya existían. Precache del service worker en `cuaderno-v68`.
- Duplicar materia y acciones rápidas (`web/app/js/sync.js`, `views.js`, `events.js`, `helpers.js`, `styles.css`): botón "Duplicar" en cada materia de Materias copia su nombre, unidades y color a una nueva ("(copia)"), abre su editor directo y copia en segundo plano (best-effort, si hay conexión) los materiales de referencia guardados en Storage — sin tocar alumnos ni historial (`duplicateSubject()`). FAB (botón flotante "+") siempre visible con las 3 acciones más repetitivas — nuevo alumno, nueva clase, registrar pago —, arriba de la barra inferior en mobile / esquina inferior derecha en escritorio; precarga lo que puede según el contexto (con una ficha abierta va directo a su pestaña; si no, pregunta primero para quién con una lista simple de alumnos). Precache del service worker en `cuaderno-v67`.
- Papelera con deshacer (`web/app/js/config.js`, `helpers.js`, `sync.js`, `views.js`, `events.js`, `styles.css`): borrar un alumno o una materia ya no los saca del todo — quedan en una papelera (`catalog.trash` para materias, con sus unidades/color/packs; el ya existente `deleted` de alumnos, ahora con ventana de 7 días en vez de 90) restaurable completa desde una nueva sección "Papelera" en Cuenta, con cuánto le queda a cada uno y botones Restaurar/Eliminar definitivo; pasados los 7 días se purgan solos al abrir la app (`trashDaysLeft()`). Patrón único de confirmación de borrado en toda la app: reemplazo inline del botón por "¿Seguro? Sí, eliminar / Cancelar" para alumno/materia/pack, y un toast con botón "Deshacer" inmediato (`toast(text, tone, undo)`) en cualquier borrado — incluidas clases, horarios, clases puntuales, pagos, simulacros y costos fijos/variables. Los materiales guardados en Storage quedan afuera de la papelera (se manejan con su propio confirm, irreversible). Precache del service worker en `cuaderno-v66`.

## [2.0.5] - 2026-07-17
- Accesibilidad y teclado (`web/app/js/*.js`, `web/app/styles.css`, `web/app/portal.html`, `web/index.html`): tokens de texto sobre fondos saturados (`--on-accent`/`--on-green`/`--on-red`) y reuso de los `-fg` ya existentes (`status-*-fg`, `tarea-*-fg`, `subj-*-fg`) donde el blanco o el color plano no llegaban a 4.5:1 (botones primarios, chips activos, badges, seña, "Ejemplo"/packs, avisos en rojo, barras de la landing y el portal); reducción de movimiento ahora cubre también toasts y el pulso del chip de sync. Navegación por teclado: atajos "/" (buscar), "N" (nuevo alumno) y "C" (nueva clase, con una ficha abierta), trampa de foco y cierre con Escape en los diálogos (buscador, nuevo alumno), foco visible agregado a enlaces. Semántica: `aria-label` en los botones de sólo ícono que tenían nada más un "×" o un glifo (borrar, semáforo, cerrar aviso), `role="progressbar"` con su valor en todas las barras de progreso/estadísticas (app y portal). Tamaños de toque ~44px en móvil para borrar/seña/portal/swatch de color/WhatsApp. Atajos listados en el nuevo centro de ayuda (paso 74). Precache del service worker en `cuaderno-v64`.
- Onboarding y ayuda (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `styles.css`): guía de "Primeros pasos" convertida en checklist de 4 pasos (crear materia → agregar alumno → registrar clase → activar portal) que se tilda sola en base a los datos reales y se oculta cuando ya están los 4 o se descarta a mano; iconitos "?" con popover corto junto a señas, política de cancelación, portal y rentabilidad (`helpTip()`, `HELP_TEXTS`); mini centro de ayuda (acordeón de preguntas frecuentes, `FAQ_ITEMS`) al final de Cuenta. Todo en el cliente, sin backend nuevo. Precache del service worker en `cuaderno-v63`.
- Color por materia e iconografía unificada (`web/app/js/config.js`, `helpers.js`, `views.js`, `events.js`, `sync.js`, `portal.js`, `styles.css`, `portal.html`): cada materia tiene un color elegible (8 opciones accesibles, `SUBJECT_COLOR_KEYS`) que se muestra siempre igual — lista de materias, editor de materia y materiales, agenda (borde + dot por evento, también en el tablero "Hoy"), barras de "tasa de aprobación"/"objetivos cumplidos" en Estadísticas y biblioteca del portal (`portal_publico`); las materias viejas sin color eligen uno estable por hash de su id, sin migrar nada a mano. Reemplazados los emojis sueltos (WhatsApp, objetivo de clase, racha, superposición de horario, resultado de objetivo sí/a medias/no, saludo del portal) por el mismo set de íconos de línea ya usado en el nav (documentado en `views.js` junto a `ICON_CHECK`), con contraste verificado en ambos temas. Precache del service worker en `cuaderno-v62`.
- Búsqueda global (`web/app/js/helpers.js`, `js/views.js`, `js/events.js`): ícono de lupa siempre visible en el nav y atajo de teclado "/" abren un overlay que busca por nombre entre alumnos, materias y materiales (`globalSearchResults`), agrupados por tipo; navegación por flechas + Enter además de click, todo local sobre `state.catalog`/`state.students` sin backend nuevo. Precache del service worker en `cuaderno-v61`.
- Rediseño del tablero como panel "Hoy" (`web/app/js/views.js`, `web/app/styles.css`): tres tarjetas arriba de todo — "Clases de hoy" (agenda del día con botón registrar/ver clase ya registrada), "Para cobrar" (reusa `cobrosAtrasadosSummary`/`vCobrosBanner`, el mismo recordatorio de cobro existente, ahora dentro de la tarjeta) y "Próximo" (exámenes dentro de 14 días, objetivos de clase por cerrar y clases de mañana) — cada una con su número grande, acceso directo a la vista completa y el estado vacío amable ya existente cuando no hay nada que mostrar; nuevos componentes `.hoy-grid`/`.hoy-card` en tarjetas, mobile-first (1 columna, 3 desde 860px) y con los mismos design tokens claro/oscuro que el resto de la app. Precache del service worker en `cuaderno-v60`.

## [2.0.4] - 2026-07-17
- Rediseño de la landing (`web/index.html`): paleta azul marino/verde-azulado, tipografías Poppins/Inter alojadas localmente en `web/fonts/` (sin CDN), header sticky, hero a dos columnas con mock del dashboard, grilla de características con íconos SVG inline y estilo nuevo para "Usala donde quieras"; conserva el selector de tema claro/oscuro/automático con variantes coherentes para ambos temas.
- Sistema visual de la app (`web/app/styles.css`): design tokens de color (claro/oscuro), tipografía Poppins/Inter local (`web/app/fonts/`, sin CDN), tamaños, radios, sombras y espaciados con la misma identidad que la landing (marino + acento teal); nuevos componentes reutilizables `.btn`/`.badge`/`.ds-*` como base para rediseñar las vistas; logo (check en cuadradito con degradé marino) aplicado al favicon, `icon-192`/`icon-512` y el `theme_color`/`background_color` del manifest. No cambia el HTML de las vistas todavía.
- Rediseño de todas las vistas de la app sobre esa base: navegación persistente con íconos (sidebar en escritorio, barra inferior en mobile) con estado activo y acceso directo al toggle de tema, en reemplazo de las pestañas/chips sueltas; encabezado de sección (eyebrow + título + acción principal) en tablero, estudiantes, ficha, pagos/rentabilidad, agenda, materias, estadísticas, cuenta y panel; logo de marca en las pantallas de login/registro/recuperación y en `portal.html` (que ahora comparte paleta y tipografía local con el resto de la app). Los documentos para imprimir (informe, contrato) quedan fuera del nav a propósito y siguen imprimiendo igual.
- Estados vacíos, feedback y microinteracciones: estados vacíos con ícono, frase y una acción clara (sin alumnos, sin clases de la semana, sin materiales, sin cobros del mes, portal apagado) en vez de espacio en blanco; toasts discretos y autodescartables tras guardar/borrar/copiar (clase, simulacro, pago, horario, resultado de examen, alta de alumno, exportar, copiar link) en reemplazo de los mensajes sueltos que quedaban pegados; chip de estado de datos (guardado/sincronizando/sin conexión/error) siempre visible en el nav, con el mismo `id="syncStatus"` que ya actualizaba `sync.js` en cada tick; skeletons con animación mientras cargan materiales, respaldos, portal y las pestañas del panel admin, en vez de un "Cargando…" seco; hover/active sutiles en tarjetas y botones, coherentes con la animación ya existente del cierre de objetivo de clase. Precache del service worker en `cuaderno-v59`.

## [2.0.3] - 2026-07-16
- Base del portal de invitados: tabla `portales` y RPC pública `portal_publico()` (migración 013), sección "Portal para tus alumnos" en Cuenta (activar/desactivar, copiar link, regenerar llave, publicar el nombre a mostrar) y página standalone `portal.html`/`js/portal.js` sin sesión y fuera del service worker.
- Biblioteca del portal: toggle "Compartir" por archivo en Materiales, "Publicar cambios" ahora firma URLs de Storage (30 días) y arma la biblioteca por materia, renovación automática silenciosa cuando quedan a 20+ días de vencer, republicado inmediato al dejar de compartir o borrar un archivo, y sección Biblioteca (agrupada, con buscador) como lo primero que ve el alumno en `portal.html`.
- Llave por alumno: card "Portal para este alumno" en la ficha (generar/copiar/regenerar/revocar su link individual) con checkboxes explícitos de qué ve (próxima clase, tarea de la última clase, avance por unidades) — nunca notas, pagos, señas ni comentarios privados; se actualiza solo al tildar o con "Publicar cambios"; en `portal.html` el alumno con llave propia ve primero su saludo y bloque personal, arriba de la biblioteca y links generales.

## [2.0.2] - 2026-07-16
- Generador de contratos de servicio: botón "Generar contrato" en la ficha del alumno, con un modelo precargado (docente, alumno, materia, tarifa y modalidad, horarios habituales, política de cancelación y señas) y campos editables (responsable, DNI opcional, fecha de inicio, cláusulas adicionales); datos del docente reutilizables cargados una sola vez en Cuenta; vista tipo documento con el mismo patrón que el informe (Descargar PDF / Copiar texto).
- Rentabilidad real: sub-pestaña dentro de Pagos con costos fijos mensuales y variables por clase (asignables a una materia o alumno puntual), $ neto por hora realmente dictada del mes (ingresos cobrados menos costos, sobre la duración cargada en cada clase), desglose por materia y por alumno, proyección del mes en curso y un histórico de 12 meses (ganancia neta y $/hora) con barras que admiten negativo y línea de tendencia, todo en CSS/SVG puro.
- Objetivos de clase: campo opcional al registrar una clase, mini-tarjeta de cierre con micro-animación al registrar la clase siguiente (o desde la ficha) para marcar si se cumplió, racha de objetivos cumplidos en la ficha, y tasa de cumplimiento en Estadísticas e informe compartible.
- Agenda con vista mensual (switch Semana/Mes, grilla del mes con mini-marcas por día, detalle de día con "Programar clase acá"); seña opcional por alumno (monto fijo o % de la tarifa) con estado por clase puntual (pendiente/cobrada/retenida/devuelta); política de cancelación configurable en Cuenta (horas mínimas de aviso, devolución o crédito a la próxima clase); avisos de seña sin cobrar al programar y al cancelar; señas como rubro propio en el resumen mensual de Pagos.
- Recordatorios de cobro: aviso desplegable en el tablero (clases sin cobrar + mensualidades vencidas + señas pendientes, con marcar-cobrada y WhatsApp por alumno), notificación del navegador opcional (permiso pedido recién al activarla) y configuración en Cuenta (sí/no, días de atraso mínimos).

## [2.0.1] - 2026-07-16
- Eliminado GoatCounter de la landing (métricas propias en `metricas_diarias`/`metricas_horarias` no se tocan).
- Límite total de materiales (20 MB por usuario entre todas sus materias), con índice liviano en el catálogo, reconciliación contra Storage y barra de uso en la sección Materiales.
- Tema elegible (automático/claro/oscuro) en la landing, con botón sol/luna en el header y la misma clave de localStorage que usa la app.
- Panel admin → Usuarios: última conexión reforzada server-side (`registrar_actividad`), orden por última conexión y botón para eliminar cuentas (con doble confirmación escribiendo el email) que usa la nueva RPC `admin_eliminar_usuario`.
- Panel admin → Actividad: valor de cada barra siempre visible (rotado en los gráficos de 30/48 puntos), etiquetas de eje abreviadas sin superponerse y total del período junto al título de cada gráfico.
- Cierre automático de cuentas inactivas (mail recordatorio a los 30 días, aviso final a los 5 meses, borrado a los 6, con modo simulacro que sólo registra en el log): cron diario `revisar_inactivos()` en el backend, mails transaccionales por Brevo, y en el Panel admin → Usuarios un chip "inactivo hace X" por fila más una nueva pestaña "Inactividad" con el log de notificaciones y cierres.

## [2.0.0] - 2026-07-16
- Registro de pagos por alumno.
- Informe de progreso compartible.
- Botones de contacto por WhatsApp.
- Carga de resultados de examen.
- Plantillas de materias para altas rápidas.
- Agenda semanal de clases.
- Landing actualizada con las características al día.

## [1.8.0] - 2026-07-15
- Login: envío con Enter y límite de intentos fallidos.
- Catálogo directo con materia de ejemplo al crear cuenta.
- Una ficha por alumno por materia (en vez de una ficha global por alumno).
- Packs de materias para altas rápidas de catálogo.
- Materiales adjuntos por materia.
- Fetch de métricas horarias para el panel de estadísticas.
- Actividad detallada por día y por hora en el panel.
- Lista de respaldos aligerada (menos payload por entrada).

## [1.7.0] - 2026-07-15
- Modo oscuro.
- Onboarding con alumno de ejemplo para cuentas nuevas.
- Buscador y filtros en la lista de alumnos.
- Cronómetro de clase y recordatorio de respaldo.

## [1.6.0] - 2026-07-15
- Estadísticas del profesor (panel agregado).
- "El aula": mapa visual de alumnos agrupados por materia.
- Vista de estadísticas por materia.

## [1.5.0] - 2026-07-15
- Panel de recursos.
- Integración de GoatCounter en la landing.
- Métricas de actividad y panel correspondiente.
- Panel admin de usuarios y presencia.
- Heartbeat de presencia de usuarios conectados.

## [1.4.0] - 2026-07-15
- Frontend separado en archivos (de script único a los seis `js/*.js` actuales).
- SQL de Supabase movido a repo propio versionado (`cuaderno-supabase`).
- Empaquetado de escritorio movido a `cuaderno-desktop` (en pausa).
- Empaquetado de Android movido a `cuaderno-android` (en pausa).
- Repo web limpiado y documentado.

## [1.3.0] - 2026-07-14
- Auditoría de seguridad.
- Roles profesor/administrador.
- Fix de ancla muerta en el aviso de nueva versión.
- Sync eficiente: solo se escribe cuando hay cambios reales.
- Landing simplificada a versión solo web.
- Auto-update de la app de escritorio (Tauri).
- Aviso de versión nueva en las apps nativas.
- Respaldos automáticos con historial.
- Techo de 24hs a la sesión web restaurada por el navegador.
- Sesión web por navegador, emails recordados y confirmación de cuenta.

## Antes de v1.3.0
- Firma de release de Android y versión 1.2.0.
- Cierre de versión 1.2.0.
- Reportes de problemas y branding genérico.
- Landing v2 y limpieza general.
- Fix de link a la app en la landing bajo `file://`.
- Referencias actualizadas al repo renombrado (`manugandini53-design.github.io`).
- Sitio del producto: landing separada de la app, app movida a `/app/`.
- Bump de versión a 1.1.0.
- Workflow de deploy automático a GitHub Pages.
- Login bloqueante y backend Supabase embebido (producto multi-tenant).
- Setup de Capacitor y proyecto Android nativo.
- Setup de Tauri: primer build de escritorio funcional (.exe/.msi).
- Versión estable de la PWA, previa a la migración a nativo.
- Carga inicial de archivos del proyecto.
